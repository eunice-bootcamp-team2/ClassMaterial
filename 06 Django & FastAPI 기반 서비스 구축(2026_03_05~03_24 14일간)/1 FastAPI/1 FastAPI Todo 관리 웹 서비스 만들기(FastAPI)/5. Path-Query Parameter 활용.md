두 코드에서 변경된 핵심은 아래 두 가지입니다. 
- 첫째, Path parameter를 사용하여 URL 경로에서 특정 Todo의 id 값을 직접 받도록 구성하였습니다. 
- 둘째, Query parameter를 사용하여 전체 조회 API에 검색, 필터, 정렬 기능을 추가하였습니다

예를 들면:
- Path parameter: URL 경로에서 값을 직접 받습니다.  
    예) `/todos/1`
    
- Query parameter: URL 뒤에 `?key=value` 형태로 값을 받습니다.  
    예) `/todos?done=true&keyword=공부`
    
- Query parameter를 활용하여 검색, 필터, 정렬 기능을 추가하였습니다.

---
코드 분석을 단계별로 할 때는, 현업에서는 보통 기능 단위로 끊어서 봅니다.  
간단히 팁을 드리면 아래 순서가 좋습니다.

`1.` 먼저 “무엇이 바뀌었는지” 한 줄로 적습니다.  
예: `전체 조회 API에 검색/필터/정렬 기능이 추가되었다.`

`2.` 그다음 “어느 파일이 바뀌었는지” 확인합니다.  
예: `app/routes/item.py`, `app/main.py`

`3.` import 변경부터 봅니다.  
실무에서는 `Query`, `Optional`, `status`처럼 import가 바뀌면 기능 확장 신호로 봅니다.

`4.` 함수 시그니처를 먼저 봅니다.  
`def get_todos(...)`의 매개변수가 늘어났다면, 입력 방식이 바뀐 것입니다.  
즉, 내부 로직보다 먼저 “API 사용법이 어떻게 달라졌는지”를 봅니다.

`5.` 마지막으로 내부 로직을 봅니다.  
검색, 필터, 정렬 순서대로 result가 어떻게 변하는지 확인하면 됩니다.  
실무에서는 이 부분을 “입력 → 처리 → 반환” 흐름으로 짧게 정리합니다.

코드 분석은 파일 단위로 보기보다, 기능 단위로 보시는 것이 좋습니다. 먼저 어떤 기능이 추가되었는지 한 줄로 정리하고, 다음으로 import 변경, 함수 매개변수 변경, 내부 처리 로직 변경 순서로 확인하면 흐름이 훨씬 잘 보입니다

---
### 코드 분석 예시

상황:  
`04 → 05 변경 (Pydantic → Path/Query 기능 추가)`

`1.` 작업 요약 (맨 위에 작성하는 문장)
`기존 Todo API에 Path parameter와 Query parameter를 적용하여, 특정 데이터 조회 기능과 검색/필터/정렬 기능을 추가하였습니다.`

`2.` 변경 포인트 분석
① 입력 방식 변경 (API 사용법 변화)
- 기존:  
    → `/todos` (단순 전체 조회)
    
- 변경:
    - `/todos/{id}` → 특정 데이터 조회 (Path parameter)
    - `/todos?done=true&keyword=공부` → 검색/필터 (Query parameter)
        
핵심 : `API를 사용하는 방식 자체가 확장됨`

② 함수 시그니처 변경
```python
def get_todos(
    done: Optional[bool] = None,
    keyword: Optional[str] = None,
    sort: Optional[str] = None
)
```
분석
- Query parameter가 함수 인자로 추가됨
- FastAPI가 자동으로 URL 값을 매핑
    
핵심 : `입력값 → 함수 파라미터로 자동 연결됨`

③ 내부 로직 변경 (데이터 처리 흐름)
```python
# 1. 전체 데이터 조회
results = todos

# 2. 필터
if done is not None:
    results = [t for t in results if t.done == done]

# 3. 검색
if keyword:
    results = [t for t in results if keyword in t.title]

# 4. 정렬
if sort == "asc":
    results = sorted(results, key=lambda x: x.id)
```

###### 분석흐름
| 단계  | 역할        |
| --- | --------- |
| 1   | 기본 데이터 로드 |
| 2   | 조건 필터링    |
| 3   | 키워드 검색    |
| 4   | 정렬 처리     |
핵심 : `데이터 처리 흐름이 "단계형 파이프라인" 구조로 변경됨`

④ Path parameter 추가
```python
@app.get("/todos/{todo_id}")
def get_todo(todo_id: int):
```
분석
- URL 경로에서 id를 직접 받음
- 특정 데이터 조회 가능
    
핵심 : `단건 조회 기능 추가`

전체 흐름 정리
입력값(Query/Path)을 기반으로 데이터를 단계적으로 필터링 및 가공하여 반환하는 구조로 개선되었습니다.

---
`app/models/item.py`
```python
from pydantic import BaseModel, Field
from typing import Optional


# 할 일 생성할 때 사용하는 데이터 형식
class TodoCreate(BaseModel):  # 요청 데이터 검증 (POST)
    title: str = Field(..., min_length=1, max_length=100, description="할 일 제목")
    done: bool = False


# 할 일 수정할 때 사용하는 데이터 형식
class TodoUpdate(BaseModel):  # 수정 데이터 검증 (PUT/PATCH)
    title: Optional[str] = Field(None, min_length=1, max_length=100, description="수정할 할 일 제목")
    done: Optional[bool] = None


# 실제 저장/응답에 사용하는 데이터 형식
class Todo(BaseModel):  # 응답 데이터 형식 정의
    id: int
    title: str = Field(..., min_length=1, max_length=100, description="할 일 제목")
    done: bool = False
```
---
`app/routes/item.py`
```python
from fastapi import APIRouter, HTTPException, status, Query  # ✅ Query 추가
from typing import Optional  # ✅ Optional 추가
from app.models.item import Todo, TodoCreate, TodoUpdate

router = APIRouter()

# 임시 저장소(DB 대신 사용)
todos = []
next_id = 1


# 전체 조회 + 검색/필터
@router.get("/todos", response_model=list[Todo])
def get_todos(
    keyword: Optional[str] = Query(
        None,
        min_length=1,
        max_length=100,
        description="제목에서 검색할 키워드"
    ),  # ✅ Query parameter - 제목 검색
    done: Optional[bool] = Query(
        None,
        description="완료 여부로 필터링 (true/false)"
    ),  # ✅ Query parameter - 완료 여부 필터
    sort: Optional[str] = Query( # ✅ 추가: 정렬 기능  
		None,  
		description="정렬 방식 (asc 또는 desc)"  
	)
):
    # ✅ 추가: 원본 리스트를 기준으로 필터링 시작
    result = todos

    # ✅ keyword가 있으면 title에 포함된 Todo만 조회
    if keyword is not None:
        result = [todo for todo in result if keyword.lower() in todo.title.lower()]

    # ✅ done 값이 있으면 완료 여부로 필터링
    if done is not None:
        result = [todo for todo in result if todo.done == done]
    
    # ✅ 추가: 정렬 기능  
	# id 기준 오름차순 정렬  
	if sort == "asc":  
		result = sorted(result, key=lambda todo: todo.id)  
  
	# ✅ 추가: 정렬 기능  
	# id 기준 내림차순 정렬  
	elif sort == "desc":  
		result = sorted(result, key=lambda todo: todo.id, reverse=True)

    return result


# 상세 조회
@router.get("/todos/{todo_id}", response_model=Todo)
def get_todo(todo_id: int):  # ✅ Path parameter 사용: todo_id
    for todo in todos:
        if todo.id == todo_id:
            return todo
    raise HTTPException(
        status_code=404,
        detail="해당 할 일이 없습니다."
    )


# 생성
@router.post("/todos", response_model=Todo, status_code=status.HTTP_201_CREATED)
def create_todo(todo_data: TodoCreate):
    global next_id

    new_todo = Todo(
        id=next_id,
        title=todo_data.title,
        done=todo_data.done
    )

    todos.append(new_todo)
    next_id += 1
    return new_todo


# 수정
@router.put("/todos/{todo_id}", response_model=Todo)
def update_todo(todo_id: int, todo_data: TodoUpdate):  # ✅ Path parameter 사용: todo_id
    for index, todo in enumerate(todos):
        if todo.id == todo_id:
            updated_todo = todo.model_copy(
                update=todo_data.model_dump(exclude_unset=True)
            )
            todos[index] = updated_todo
            return updated_todo

    raise HTTPException(
        status_code=404,
        detail="수정할 할 일이 없습니다."
    )


# 삭제
@router.delete("/todos/{todo_id}")
def delete_todo(todo_id: int):  # ✅ Path parameter 사용: todo_id
    for index, todo in enumerate(todos):
        if todo.id == todo_id:
            deleted_todo = todos.pop(index)
            return {
                "message": "삭제 완료",
                "deleted": deleted_todo
            }

    raise HTTPException(
        status_code=404,
        detail="삭제할 할 일이 없습니다."
    )
```

https://www.figma.com/design/T6Hf8Uww5OqyGYBU144pak/%EC%BD%94%EB%93%9C%EB%B6%84%EC%84%9D?node-id=43-165&t=Ehy63pt1AJUSy3RV-0
![[Pasted image 20260314181026.png]]
Query 파라미터 추가 (검색 / 필터 / 정렬) 코드를 추가하였습니다.

기존코드는 
- `/todos` 요청 시 모든 Todo 목록만 단순 조회했습니다.

추가된 코드는
- `/todos` 요청 시 검색, 필터링, 정렬 기능을 사용할 수 있게 되었습니다.

추가된 Query 파라미터는 다음과 같습니다.
① keyword (검색)
```python
keyword: Optional[str] = Query(
    None,
    min_length=1,
    max_length=100,
    description="제목에서 검색할 키워드"
)
```
Todo title에서 특정 문자열 검색 : `/todos?keyword=study`

② done (완료 여부 필터)
```python
done: Optional[bool] = Query(
    None,
    description="완료 여부 필터링 (true/false)"
)
```
완료된 Todo / 미완료 Todo 필터링 : `/todos?done=true`

③ sort (정렬)
```python
sort: Optional[str] = Query(
    None,
    description="정렬 방식 (asc 또는 desc)"
)
```
Todo id 기준 정렬 : `/todos?sort=asc` -> `/todos?sort=desc`

필터 로직 추가
	Query 파라미터를 실제로 적용하는 코드가 추가되었습니다.

keyword 검색 → 제목에 keyword가 포함된 Todo만 반환합니다.
```python
result = [todo for todo in result if keyword.lower() in todo.title.lower()]
```

추가된 코드
- 검색 (keyword)
- 완료 여부 필터 (done)
- 정렬 (sort)

---
`app/main.py`
```python
from fastapi import FastAPI
from app.routes.item import router as item_router

app = FastAPI(
    title="FastAPI Todo API",
    description="Pydantic 검증과 Path / Query Parameter가 포함된 Todo CRUD API",  # ✅ 수정: 설명 문구 변경
    version="1.1.0"  # ✅ 수정: 버전 변경
)

# 라우터 등록
app.include_router(item_router)

@app.get("/")
def home():
    return {"message": "FastAPI Todo API 실행 중"}
```
---
#### 수정된 파일 요약

`app/routes/item.py`
- `Query` import 추가
- `Optional` import 추가
- `GET /todos`에 Query parameter 추가
    - `keyword`
    - `done`
- `GET /todos/{todo_id}`, `PUT /todos/{todo_id}`, `DELETE /todos/{todo_id}`는  
    Path parameter 예시로 그대로 활용
    
`app/main.py`
- Swagger 설명 문구와 버전만 약간 수정
    
---
### Swagger에서 테스트

`1.` Path parameter 테스트
`상세 조회`
GET /todos/1
여기서 `1`이 Path parameter입니다.

`수정`
PUT /todos/1
여기서도 `1`이 Path parameter입니다.

`삭제`
DELETE /todos/1
여기서도 `1`이 Path parameter입니다.

---
`2.` Query parameter 테스트
`전체 조회`
GET /todos
그냥 전체 목록 조회입니다.

완료 여부 필터
GET /todos?done=true
완료된 Todo만 조회합니다.
GET /todos?done=false
미완료 Todo만 조회합니다.

제목 검색
GET /todos?keyword=공부
제목에 `공부`가 포함된 Todo만 조회합니다.

검색 + 필터 함께 사용
GET /todos?keyword=공부&done=false
제목에 `공부`가 들어가고, 아직 완료되지 않은 Todo만 조회합니다.

---
`3.` 오름차순 정렬
```bash
GET /todos?sort=asc
```

내림차순 정렬
```bash
GET /todos?sort=desc
```

검색 + 필터 + 정렬 같이 사용
```bash
GET /todos?keyword=공부&done=false&sort=desc
```
의미:
- 제목에 `공부` 포함
- 완료되지 않은 것만
- id 내림차순 정렬
