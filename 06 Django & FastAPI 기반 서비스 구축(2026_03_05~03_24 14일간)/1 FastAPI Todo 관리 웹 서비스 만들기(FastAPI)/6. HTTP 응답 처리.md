API가 단순히 동작만 하는 것이 아니라,
- 성공하면 어떤 상태코드를 반환하는지
- 실패하면 어떤 예외를 발생시키는지
- 사용자에게 어떤 에러 메시지를 보여줄지를 더 명확하게 정리하는 단계입니다.

`app/routes/item.py`
```python
from fastapi import APIRouter, HTTPException, status, Query
from typing import Optional
from app.models.item import Todo, TodoCreate, TodoUpdate

router = APIRouter()

# 임시 저장소(DB 대신 사용)
todos = []
next_id = 1


# 전체 조회 + 검색/필터
@router.get(
    "/todos",
    response_model=list[Todo],
    status_code=status.HTTP_200_OK,  # ✅ 추가: 상태 코드 명시
    responses={                      # ✅ 추가: Swagger에 예외 응답 문서화
        200: {"description": "전체 Todo 목록 조회 성공"},
        400: {"description": "잘못된 정렬 값 요청"}
    }
)
def get_todos(
    keyword: Optional[str] = Query(
        None,
        min_length=1,
        max_length=100,
        description="제목에서 검색할 키워드"
    ),
    done: Optional[bool] = Query(
        None,
        description="완료 여부로 필터링 (true/false)"
    ),
    sort: Optional[str] = Query(
        None,
        description="정렬 방식 (asc 또는 desc)"
    )
):
    # 원본 리스트를 기준으로 필터링 시작
    result = todos

    # keyword가 있으면 title에 포함된 Todo만 조회
    if keyword is not None:
        result = [todo for todo in result if keyword.lower() in todo.title.lower()]

    # done 값이 있으면 완료 여부로 필터링
    if done is not None:
        result = [todo for todo in result if todo.done == done]

    # ✅ 추가: 예외 처리 - 잘못된 정렬 값 검사
    if sort is not None and sort not in ["asc", "desc"]:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="sort 값은 asc 또는 desc만 사용할 수 있습니다."
        )

    # id 기준 오름차순 정렬
    if sort == "asc":
        result = sorted(result, key=lambda todo: todo.id)

    # id 기준 내림차순 정렬
    elif sort == "desc":
        result = sorted(result, key=lambda todo: todo.id, reverse=True)

    return result


# 상세 조회
@router.get(
    "/todos/{todo_id}",
    response_model=Todo,
    status_code=status.HTTP_200_OK,  # ✅ 추가: 상태 코드 명시
    responses={                      # ✅ 추가: Swagger에 예외 응답 문서화
        200: {"description": "Todo 상세 조회 성공"},
        404: {"description": "해당 Todo를 찾을 수 없음"}
    }
)
def get_todo(todo_id: int):
    for todo in todos:
        if todo.id == todo_id:
            return todo
    raise HTTPException(
        status_code=status.HTTP_404_NOT_FOUND,  # ✅ 추가: status 상수 사용
        detail="해당 할 일이 없습니다."
    )


# 생성
@router.post(
    "/todos",
    response_model=Todo,
    status_code=status.HTTP_201_CREATED,
    responses={                      # ✅ 추가: Swagger에 예외 응답 문서화
        201: {"description": "Todo 생성 성공"},
        400: {"description": "중복된 제목 또는 잘못된 요청"}
    }
)
def create_todo(todo_data: TodoCreate):
    global next_id

    # ✅ 추가: 예외 처리 - 같은 제목의 Todo 중복 생성 방지
    for todo in todos:
        if todo.title == todo_data.title:
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail="같은 제목의 할 일은 이미 존재합니다."
            )

    new_todo = Todo(
        id=next_id,
        title=todo_data.title,
        done=todo_data.done
    )

    todos.append(new_todo)
    next_id += 1
    return new_todo


# 수정
@router.put(
    "/todos/{todo_id}",
    response_model=Todo,
    status_code=status.HTTP_200_OK,  # ✅ 추가: 상태 코드 명시
    responses={                      # ✅ 추가: Swagger에 예외 응답 문서화
        200: {"description": "Todo 수정 성공"},
        400: {"description": "수정할 데이터가 없거나 잘못된 요청"},
        404: {"description": "수정할 Todo를 찾을 수 없음"}
    }
)
def update_todo(todo_id: int, todo_data: TodoUpdate):
    # ✅ 추가: 예외 처리 - 아무 값도 안 보냈을 때
    update_data = todo_data.model_dump(exclude_unset=True)
    if not update_data:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="수정할 데이터를 하나 이상 보내주세요."
        )

    for index, todo in enumerate(todos):
        if todo.id == todo_id:
            updated_todo = todo.model_copy(
                update=update_data
            )
            todos[index] = updated_todo
            return updated_todo

    raise HTTPException(
        status_code=status.HTTP_404_NOT_FOUND,  # ✅ 추가: status 상수 사용
        detail="수정할 할 일이 없습니다."
    )


# 삭제
@router.delete(
    "/todos/{todo_id}",
    status_code=status.HTTP_200_OK,  # ✅ 추가: 상태 코드 명시
    responses={                      # ✅ 추가: Swagger에 예외 응답 문서화
        200: {"description": "Todo 삭제 성공"},
        404: {"description": "삭제할 Todo를 찾을 수 없음"}
    }
)
def delete_todo(todo_id: int):
    for index, todo in enumerate(todos):
        if todo.id == todo_id:
            deleted_todo = todos.pop(index)
            return {
                "message": "삭제 완료",
                "deleted": deleted_todo
            }

    raise HTTPException(
        status_code=status.HTTP_404_NOT_FOUND,  # ✅ 추가: status 상수 사용
        detail="삭제할 할 일이 없습니다."
    )
```

`app/main.py`
```python
from fastapi import FastAPI, Request  # ✅ 추가: Request import
from fastapi.responses import JSONResponse  # ✅ 추가: JSONResponse import
from fastapi.exceptions import RequestValidationError  # ✅ 추가: 검증 예외 import
from app.routes.item import router as item_router

app = FastAPI(
    title="FastAPI Todo API",
    description="Pydantic 검증과 Path / Query Parameter가 포함된 Todo CRUD API",
    version="1.2.0"  # ✅ 추가: 버전 수정
)

# 라우터 등록
app.include_router(item_router)


@app.get("/")
def home():
    return {"message": "FastAPI Todo API 실행 중"}


# ✅ 추가: 요청 데이터 검증 실패 시 에러 메시지 통일
@app.exception_handler(RequestValidationError)
async def validation_exception_handler(request: Request, exc: RequestValidationError):
    return JSONResponse(
        status_code=422,
        content={
            "message": "입력값 검증에 실패했습니다.",
            "errors": exc.errors()
        }
    )
```
---
(1) 상태 코드 명시
각 API에 성공 시 반환할 상태 코드를 더 분명히 적었습니다.

예를 들면:
- 전체 조회: `200 OK`
- 상세 조회: `200 OK`
- 생성: `201 Created`
- 수정: `200 OK`
- 삭제: `200 OK`
    
이렇게 해서 API가 성공했을 때 어떤 상태코드를 주는지 코드에서 바로 보이게 만들었습니다.

---
(2) 예외 처리 추가

잘못된 요청이 들어왔을 때 그냥 실패하는 것이 아니라,  
의도적으로 `HTTPException`을 발생시켜 **정확한 에러 상황**을 알려주도록 했습니다.

추가한 예외 처리 예시는 다음과 같습니다.
- `sort=abc`처럼 잘못된 정렬 값 → `400 Bad Request`
- 같은 제목의 Todo를 또 생성 → `400 Bad Request`
- 수정할 데이터 없이 PUT 요청 → `400 Bad Request`
- 존재하지 않는 Todo 조회/수정/삭제 → `404 Not Found`
    
---
(3) 에러 메시지 명확화
에러가 발생했을 때 사용자에게 보여줄 메시지를 분명하게 작성했습니다.

예:
```json
{  
  "detail": "sort 값은 asc 또는 desc만 사용할 수 있습니다."  
}
```

또는
```json
{  
  "detail": "수정할 데이터를 하나 이상 보내주세요."  
}
```

그리고 Pydantic 검증 실패(예: title이 빈 문자열)도 `main.py`에서 예외 핸들러를 추가하여 조금 더 정리된 형태로 내려가도록 했습니다.

---
(4) Swagger 문서에 응답 정보 표시
각 API의 `responses={...}` 옵션을 추가해서  
Swagger 문서에서도
- 성공 응답
- 실패 응답
    
설명이 보이도록 만들었습니다.

---
### Swagger에서 테스트

이번 단계는 성공 테스트 + 실패 테스트를 같이 해보는 것이 중요합니다.

---
`1.` 성공 상태코드 테스트

전체 조회
GET /todos

확인할 것:
- 상태코드 `200`
    
생성
POST /todos

요청 예시:
```json
{  
  "title": "FastAPI 공부하기",  
  "done": false  
}
```

확인할 것:
- 상태코드 `201`
    
수정
```
PUT /todos/1
```

요청 예시:
```json
{  
  "title": "FastAPI 복습하기"  
}
```

확인할 것:
- 상태코드 `200`
    
삭제
```
DELETE /todos/1
```

확인할 것:
- 상태코드 `200`
    
---
`2.` 예외 처리 테스트

없는 Todo 상세 조회
```
GET /todos/999
```

확인할 것:
- 상태코드 `404`
- 메시지:
    
```json
{  
  "detail": "해당 할 일이 없습니다."  
}
```

---
없는 Todo 수정
```
PUT /todos/999
```

요청 예시:
```json
{  
  "title": "수정 테스트"  
}
```

확인할 것:
- 상태코드 `404`
- 메시지:
    
```json
{  
  "detail": "수정할 할 일이 없습니다."  
}
```

---
없는 Todo 삭제
```
DELETE /todos/999
```

확인할 것:
- 상태코드 `404`
- 메시지:
    
```json
{  
  "detail": "삭제할 할 일이 없습니다."  
}
```
---
`3.` 잘못된 요청 테스트

잘못된 sort 값 테스트
```
GET /todos?sort=abc
```

확인할 것:
- 상태코드 `400`
- 메시지:
    
```json
{  
  "detail": "sort 값은 asc 또는 desc만 사용할 수 있습니다."  
}
```

---
중복 제목 생성 테스트
먼저 하나 생성합니다.
```json
{  
  "title": "장보기",  
  "done": false  
}
```

그다음 똑같이 다시 생성합니다.
```json
{  
  "title": "장보기",  
  "done": true  
}
```

확인할 것:
- 상태코드 `400`
- 메시지:
    
```json
{  
  "detail": "같은 제목의 할 일은 이미 존재합니다."  
}
```

---
수정 데이터 없이 PUT 요청
```
PUT /todos/1
```

요청 본문:
```json
{}
```

확인할 것:
- 상태코드 `400`
- 메시지:
```json
{  
  "detail": "수정할 데이터를 하나 이상 보내주세요."  
}
```

---
`4.` 검증 실패 테스트

title 빈 문자열
```
POST /todos
```

요청 본문:
```json
{  
  "title": "",  
  "done": false  
}
```

확인할 것:
- 상태코드 `422`
- 메시지 안에 검증 오류 정보 포함
    