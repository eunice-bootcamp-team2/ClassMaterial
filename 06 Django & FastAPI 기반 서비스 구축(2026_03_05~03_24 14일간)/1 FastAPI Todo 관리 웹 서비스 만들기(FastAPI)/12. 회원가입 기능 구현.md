디렉토리 구조
```
fastapi_todo/
├── app/
│   ├── main.py
│   ├── core/
│   │   ├── __init__.py
│   │   └── settings.py
│   ├── db/
│   │   ├── __init__.py
│   │   ├── base.py
│   │   ├── session.py
│   │   └── init_db.py
│   ├── models/
│   │   ├── __init__.py
│   │   ├── todo.py
│   │   └── user.py          # ✅ 추가
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── todo.py
│   │   └── user.py          # ✅ 추가
│   ├── routes/
│   │   ├── __init__.py
│   │   ├── item.py
│   │   └── user.py          # ✅ 추가
│   └── templates/
│       └── index.html
├── static/
│   └── style.css
└── todo.db
```

파일 및 폴더 생성
```bash
touch app/models/user.py 
touch app/schemas/user.py
touch app/routes/user.py  
```

`app/models/user.py`
```python
# ✅ 추가: User DB 모델

from sqlalchemy import Column, Integer, String
from app.db.base import Base


class UserDB(Base):
    __tablename__ = "users"   # ✅ 추가: users 테이블 생성

    id = Column(Integer, primary_key=True, index=True)
    username = Column(String(50), unique=True, nullable=False, index=True)
    password = Column(String(255), nullable=False)   # ✅ 추가: 해시된 비밀번호 저장
```
모델을 분리하는 이유는 유지보수 때문에 하는 것입니다.
```python
# app/models/models.py

class TodoDB(Base):
    __tablename__ = "todos"

    id = Column(Integer, primary_key=True)
    title = Column(String(100))
    done = Column(Boolean)


class UserDB(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True)
    username = Column(String(50))
    password = Column(String(255))
```
이렇게 한 파일에 여러 테이블을 작성해도 문제 없습니다. 그러나 앱 하나에 모델 하나의 파일을 작성하는 경우가 많습니다. DRF도 마찬가지 입니다. 앱 한개에 각각의 모델이 있듯이 역할이 다르므로 추후 유지보수를 위해 분리하는 것을 추천합니다.

---
`app/schemas/user.py`
```python
# ✅ 추가: User 요청/응답 스키마

from pydantic import BaseModel, Field


# 회원가입 요청용
class UserCreate(BaseModel):
    username: str = Field(..., min_length=2, max_length=50, description="회원 아이디")
    password: str = Field(..., min_length=4, max_length=100, description="회원 비밀번호")


# 회원가입 응답용
class User(BaseModel):
    id: int
    username: str

    class Config:
        from_attributes = True
```

`app/routes/user.py`
```python
# ✅ 추가: 회원가입 API

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session
import hashlib

from app.db.session import get_db
from app.models.user import UserDB
from app.schemas.user import UserCreate, User

router = APIRouter(prefix="/users", tags=["Users"])   # ✅ 추가


# ✅ 추가: 비밀번호 해시 함수
def hash_password(password: str) -> str:
    return hashlib.sha256(password.encode("utf-8")).hexdigest()


# ✅ 추가: 회원가입 API
@router.post(
    "/signup",
    response_model=User,
    status_code=status.HTTP_201_CREATED,
    responses={
        201: {"description": "회원가입 성공"},
        400: {"description": "이미 존재하는 사용자명"}
    }
)
def signup(user_data: UserCreate, db: Session = Depends(get_db)):
    # 같은 username 존재 여부 확인
    existing_user = db.query(UserDB).filter(UserDB.username == user_data.username).first()
    if existing_user:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="이미 사용 중인 username입니다."
        )

    # 비밀번호 해시 처리
    hashed_pw = hash_password(user_data.password)

    # DB 저장용 객체 생성
    new_user = UserDB(
        username=user_data.username,
        password=hashed_pw
    )

    db.add(new_user)
    db.commit()
    db.refresh(new_user)

    return new_user
```
---
`app/db/init_db.py`
```python
from app.db.base import Base
from app.db.session import engine

from app.models.todo import TodoDB   # noqa: F401
from app.models.user import UserDB   # noqa: F401


def init_db():
    Base.metadata.create_all(bind=engine)
```
VSCode / Pylance 기준으로 보면
- `TodoDB`
- `UserDB` 이 코드에서 두 모델은 이 파일에서 직접 사용되지 않습니다.
```python
def init_db():
    Base.metadata.create_all(bind=engine)
```
그래서 에디터는 이 import는 사용되지 않았다 라고 판단해서 비활성화 처리합니다

그러나 SQLAlchemy에서는 모델을 import해야 Base에 등록됩니다.
즉 이코드 때문입니다.
```python
class UserDB(Base):
```
여기서
```python
Base
```
에 모델 정보가 등록됩니다. 그래서
```python
Base.metadata.create_all()
```
이 실행될 때
- Todo 테이블
- User 테이블을 자동으로 생성합니다.

`F401`의 의미 : flake8 검사 코드
```
F401 = import 했지만 사용하지 않은 변수
```
만약 주석문 처리를 하지 않으면 import는 필요 없는 코드로 인식하여 경고를 표시합니다.

---
`app/main.py`
```python
from fastapi import FastAPI, Request
from fastapi.responses import JSONResponse
from fastapi.exceptions import RequestValidationError
from fastapi.middleware.cors import CORSMiddleware
from fastapi.staticfiles import StaticFiles
from fastapi.templating import Jinja2Templates

from app.routes.item import router as item_router
from app.routes.user import router as user_router   # ✅ 추가

from app.core.settings import settings
from app.db.init_db import init_db

init_db()

app = FastAPI(
    title=settings.PROJECT_NAME,
    description="FastAPI + SQLAlchemy 기반 Todo CRUD API",
    version=settings.VERSION
)

app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.ALLOWED_ORIGINS,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(item_router)
app.include_router(user_router)   # ✅ 추가: 회원가입 라우터 등록

app.mount("/static", StaticFiles(directory="static"), name="static")

templates = Jinja2Templates(directory="app/templates")


@app.get("/")
def home():
    return {"message": "FastAPI Todo API 실행 중 (회원가입 기능 추가 완료)"}


@app.get("/page")
def todo_page(request: Request):
    return templates.TemplateResponse(
        "index.html",
        {"request": request}
    )


@app.exception_handler(RequestValidationError)
async def validation_exception_handler(request: Request, exc: RequestValidationError):
    return JSONResponse(
        status_code=422,
        content={
            "message": "입력값 검증에 실패했습니다.",
            "errors": exc.errors()
        }
    )
```
---
### 이번에 추가한 핵심은 3개예요.

(1) User 모델 추가
`app/models/user.py`
- `users` 테이블 생성
- `id`, `username`, `password` 컬럼 구성
    
(2) User 스키마 추가
`app/schemas/user.py`
- 회원가입 요청 형식 정의
- 응답 형식 정의
    
(3) 회원가입 API 추가
`app/routes/user.py`
- `POST /users/signup`
- username 중복 검사
- 비밀번호 해시 후 저장
- 가입 완료된 사용자 정보 반환
    
---
### Swagger에서 테스트
POST /users/signup

실제 URL:
http://127.0.0.1:8000/users/signup

---
Swagger 테스트

서버 실행:
```
uvicorn app.main:app --reload
```

Swagger 문서:
http://127.0.0.1:8000/docs

들어가서 `Users` 섹션의 `POST /users/signup`를 눌러 테스트하면 됩니다.

테스트 예시 JSON
```json
{  
  "username": "eunice",  
  "password": "1234"  
}
```

성공 응답 예시
```json
{  
  "id": 1,  
  "username": "eunice"  
}
```

---
테스트할 때 확인할 것

성공 케이스
```json
{  
  "username": "eunice",  
  "password": "1234"  
}
```

- 201 Created 나오는지
- 응답에 `id`, `username`만 보이는지
- `password`는 응답에 안 보이는지
    
중복 케이스

같은 username으로 한 번 더 가입:
```json
{  
  "username": "eunice",  
  "password": "9999"  
}
```

- 400 에러 나오는지
- `"이미 사용 중인 username입니다."` 나오는지
    

유효성 검사 케이스

너무 짧게 입력:
```json
{  
  "username": "a",  
  "password": "12"  
}
```
- 422 에러 나오는지 확인
    
---
DB에 실제로 뭐가 저장되나
예를 들어 가입하면 DB에는 이런 느낌으로 저장됩니다.

|id|username|password|
|---|---|---|
|1|eunice|sha256로 해시된 문자열|

즉, 입력한 비밀번호 원문 `"1234"`가 그대로 저장되는 게 아니라,  
해시된 값으로 저장돼요.

다만 이 방식은 학습용 간단 버전이고,  
실무에서는 보통 `bcrypt`나 `passlib`를 더 많이 씁니다.
