# 자가정검 체크리스트(디코나이)

## 체크 방법

- `[ ]` 미확인 / 미구현
- `[x]` 확인 완료 / 발표 가능

## 주의사항

- 체크한 항목은 발표에서 실제로 시연 가능해야 합니다.
- 코드가 있다가 아니라 실제로 동작한다를 기준으로 체크합니다.
- 4차 발표는 3차처럼 화면만 보이는 것이 아니라 **AI + 비동기 + 운영 + 모니터링 구조가 연결되어 있는지**를 기준으로 점검합니다.

---

## 1. 프로젝트 소개 / 문제 정의

- [ ]  우리 프로젝트가 해결하려는 산업 현장 문제가 무엇인지 설명할 수 있다.
- [ ]  왜 관제 플랫폼이 필요한지 설명할 수 있다.
- [ ]  단순 모니터링이 아니라 사전 감지/예측이 왜 중요한지 설명할 수 있다.
- [ ]  3차와 4차의 차이를 한 문장으로 설명할 수 있다.
- [ ]  4차 발표의 범위를 한 문장으로 설명할 수 있다.

예:

- [ ]  “3차는 UI + 데이터 흐름 + 지오펜스 + 알람을 보이게 만든 단계이고, 4차는 AI + 비동기 + 모니터링 + 배포 구조로 운영 가능한 시스템을 만드는 단계입니다.” 라고 설명 가능하다.

---

## 2. 시스템 구조 설명

- [ ]  FastAPI 센서 서버의 역할을 설명할 수 있다.
- [ ]  Redis의 역할을 설명할 수 있다.
- [ ]  Django + DRF 관제 서버의 역할을 설명할 수 있다.
- [ ]  Celery의 역할을 설명할 수 있다.
- [ ]  FastAPI AI 서버의 역할을 설명할 수 있다.
- [ ]  PostgreSQL의 역할을 설명할 수 있다.
- [ ]  WebSocket 또는 알림 구조의 역할을 설명할 수 있다.
- [ ]  Prometheus의 역할을 설명할 수 있다.
- [ ]  Grafana의 역할을 설명할 수 있다.
- [ ]  Docker Compose의 역할을 설명할 수 있다.
- [ ]  전체 데이터 흐름을 순서대로 설명할 수 있다.

전체 흐름 예시:

```
센서 데이터 → Redis → Django 저장/판단 → Celery 비동기 작업 → FastAPI AI 분석 → 결과 저장 → 알람/대시보드 출력 → Prometheus/Grafana 모니터링
```

---

## 3. 핵심 기능 자가점검

### 3-1. AI 이상 탐지

- [ ]  AI 분석 대상 센서 데이터가 정해져 있다.
- [ ]  ARIMA 또는 이상 탐지 로직이 구현되어 있다.
- [ ]  정상 데이터와 이상 데이터를 구분할 수 있다.
- [ ]  AI 분석 결과가 DB에 저장된다.
- [ ]  AI 분석 결과를 API 또는 화면에서 조회할 수 있다.
- [ ]  발표에서 AI 분석 결과를 직접 보여줄 수 있다.
- [ ]  AI 결과가 알람 또는 위험도 판단 흐름과 연결되어 있다.

---

### 3-2. Celery 비동기 처리

- [ ]  Celery 설정이 프로젝트에 적용되어 있다.
- [ ]  Redis가 Celery broker로 연결되어 있다.
- [ ]  AI 분석 또는 무거운 작업이 Celery task로 분리되어 있다.
- [ ]  사용자 요청 후 즉시 응답하고, 백그라운드에서 처리가 진행된다.
- [ ]  Celery worker 실행 방법이 문서화되어 있다.
- [ ]  worker 실행 로그에서 task 수행을 확인할 수 있다.
- [ ]  실패한 task가 FAIL 상태로 기록된다.

---

### 3-3. Task 상태 관리

- [ ]  Task 상태 모델 또는 상태 관리 구조가 있다.
- [ ]  PENDING 상태가 기록된다.
- [ ]  PROCESSING 상태가 기록된다.
- [ ]  SUCCESS 상태가 기록된다.
- [ ]  FAIL 상태가 기록된다.
- [ ]  task_id 기반 상태 조회 API가 있다.
- [ ]  실패 시 에러 메시지 또는 원인을 확인할 수 있다.
- [ ]  발표에서 task 상태 변화 흐름을 설명할 수 있다.

---

### 3-4. Redis 도입

- [ ]  Redis가 Docker Compose 또는 로컬 환경에서 실행된다.
- [ ]  Redis가 Celery broker로 연결되어 있다.
- [ ]  Redis를 Pub/Sub 또는 실시간 데이터 처리 안정화에 사용했다.
- [ ]  Redis 연결 실패 시 어떤 문제가 생기는지 설명할 수 있다.
- [ ]  Redis가 왜 필요한지 한 문장으로 설명할 수 있다.

---

### 3-5. 실시간 데이터 / 알림 고도화

- [ ]  센서 데이터가 실시간 또는 주기적으로 유입된다.
- [ ]  유입된 데이터가 Django 또는 DB에 반영된다.
- [ ]  위험 조건 발생 시 알림이 발생한다.
- [ ]  WebSocket 알림 또는 상태 조회 방식 중 하나가 구현되어 있다.
- [ ]  Discord / Slack 등 외부 알림을 구현했다. `(선택)`
- [ ]  동일 위험 조건에서 알람이 과도하게 반복 생성되지 않는다.
- [ ]  알람 이력 또는 이벤트 로그를 조회할 수 있다.

---

### 3-6. 모니터링

- [ ]  Prometheus가 실행된다.
- [ ]  Django 메트릭을 수집할 수 있다.
- [ ]  FastAPI 메트릭을 수집할 수 있다.
- [ ]  Celery 또는 task 관련 지표를 확인할 수 있다. `(선택)`
- [ ]  Grafana가 실행된다.
- [ ]  Grafana 대시보드가 구성되어 있다.
- [ ]  요청 수를 확인할 수 있다.
- [ ]  응답 시간을 확인할 수 있다.
- [ ]  에러율을 확인할 수 있다.
- [ ]  AI 처리 시간을 확인할 수 있다.
- [ ]  알람 발생 수 또는 task 처리 시간을 확인할 수 있다.
- [ ]  발표에서 “어디가 느린지 / 어디가 문제인지” 지표로 설명할 수 있다.
- [ ]  특정 지표를 기반으로 어디가 느린지(병목)를 설명할 수 있다.

---

### 3-7. Docker 구성

- [ ]  Dockerfile이 준비되어 있다.
- [ ]  docker-compose.yml이 준비되어 있다.
- [ ]  Django 서비스가 컨테이너로 실행된다.
- [ ]  FastAPI 센서 서버가 컨테이너로 실행된다.
- [ ]  FastAPI AI 서버가 컨테이너로 실행된다.
- [ ]  Redis 서비스가 컨테이너로 실행된다.
- [ ]  PostgreSQL 서비스가 컨테이너로 실행된다.
- [ ]  Celery worker가 컨테이너로 실행된다.
- [ ]  Prometheus 서비스가 컨테이너로 실행된다.
- [ ]  Grafana 서비스가 컨테이너로 실행된다.
- [ ]  `.env` 또는 환경변수 설정 방법이 문서화되어 있다.
- [ ]  `docker compose up` 기준 실행 방법을 설명할 수 있다.

---

### 3-8. Kubernetes / 운영 확장 `(선택 또는 고도화)`

- [ ]  Kubernetes 적용 여부를 설명할 수 있다.
- [ ]  Namespace 구조를 설명할 수 있다.
- [ ]  Deployment 역할을 설명할 수 있다.
- [ ]  Service 역할을 설명할 수 있다.
- [ ]  Ingress 역할을 설명할 수 있다.
- [ ]  ConfigMap / Secret 분리 이유를 설명할 수 있다.
- [ ]  HPA 자동 확장 개념을 설명할 수 있다.
- [ ]  실제 K8s 배포가 되어 있다. `(선택)`
- [ ]  장애 복구 또는 Pod 재시작 흐름을 설명할 수 있다. `(선택)`

---

## 4. 데이터 흐름 완성도

- [ ]  센서 데이터 생성 또는 수집 흐름이 동작한다.
- [ ]  센서 데이터가 Redis 또는 서버로 전달된다.
- [ ]  Django가 데이터를 저장하거나 판단한다.
- [ ]  Celery가 AI 분석 작업을 비동기로 실행한다.
- [ ]  FastAPI AI 서버가 분석 결과를 반환한다.
- [ ]  분석 결과가 DB에 저장된다.
- [ ]  저장된 결과가 대시보드 또는 API에서 조회된다.
- [ ]  위험 결과가 알람 또는 이벤트로 연결된다.
- [ ]  Prometheus / Grafana에서 관련 지표를 확인할 수 있다.
- [ ]  발표용 핵심 시연 흐름을 처음부터 끝까지 1회 이상 점검했다.
- [ ]  센서 데이터 → AI 분석 → 결과 저장 → 알람 → 모니터링까지 하나의 흐름으로 이어진다.

---

## 5. 팀 역할 설명

- [ ]  우리 팀은 데이터 흐름 기준 / 도메인 기준 중 어떤 방식으로 역할을 나눴는지 설명할 수 있다.
- [ ]  각 팀원이 자신의 담당 범위를 설명할 수 있다.
- [ ]  AI 담당자가 분석 대상, 모델, 결과 저장 방식을 설명할 수 있다.
- [ ]  비동기 담당자가 Celery + Redis 흐름을 설명할 수 있다.
- [ ]  관제 서버 담당자가 Django 저장/판단/API 구조를 설명할 수 있다.
- [ ]  모니터링 담당자가 Prometheus/Grafana 지표 의미를 설명할 수 있다.
- [ ]  인프라 담당자가 Docker Compose 또는 Kubernetes 구조를 설명할 수 있다.
- [ ]  팀 역할 설명이 기능 나열이 아니라 데이터 흐름 기준으로 연결되어 있다.

---

## 6. 발표 자료 준비 상태

- [ ]  프로젝트 개요 1장이 준비되어 있다.
- [ ]  3차 → 4차 확장 방향 자료가 준비되어 있다.
- [ ]  전체 시스템 구조도 1장이 준비되어 있다.
- [ ]  데이터 흐름 설명 자료가 준비되어 있다.
- [ ]  AI 분석 흐름 자료가 준비되어 있다.
- [ ]  비동기 처리 흐름 자료가 준비되어 있다.
- [ ]  Task 상태 관리 자료가 준비되어 있다.
- [ ]  Docker Compose 서비스 구성 자료가 준비되어 있다.
- [ ]  Prometheus / Grafana 화면 자료가 준비되어 있다.
- [ ]  알람 발생 흐름 설명 자료가 준비되어 있다.
- [ ]  실행 방법 문서가 준비되어 있다.
- [ ]  발표용 테스트 데이터 또는 위험 발생 시나리오가 준비되어 있다.

있으면 좋은 자료:

- [ ]  ERD 또는 확장 ERD
- [ ]  API 명세서
- [ ]  task 상태 API 예시
- [ ]  AI 분석 결과 테이블 예시
- [ ]  PromQL 예시
- [ ]  Docker Compose 실행 명령어
- [ ]  Kubernetes 구조도
- [ ]  트러블슈팅 사례 1개

---

## 7. 실행 가능성 / 안정성

- [ ]  발표 직전에 Docker Compose로 전체 서비스를 실행해봤다.
- [ ]  Django 서버가 정상 실행된다.
- [ ]  FastAPI 센서 서버가 정상 실행된다.
- [ ]  FastAPI AI 서버가 정상 실행된다.
- [ ]  Redis가 정상 실행된다.
- [ ]  PostgreSQL이 정상 실행된다.
- [ ]  Celery worker가 정상 실행된다.
- [ ]  Prometheus가 정상 실행된다.
- [ ]  Grafana가 정상 실행된다.
- [ ]  핵심 시연 중 500 오류가 발생하지 않는다.
- [ ]  AI 분석 실패 시에도 시스템 전체가 멈추지 않는다.
- [ ]  실패 상태 또는 에러 로그를 확인할 수 있다.
- [ ]  발표 중 문제가 생겼을 때 설명할 수 있는 대체 화면 또는 캡처가 준비되어 있다.

---

## 8. 가산점 자가확인

아래 항목은 구현 + 시연 + 설명이 모두 가능할 때만 체크합니다.

- [ ]  ARIMA 외 Isolation Forest, Z-score, Change Point 등 이상 탐지 조합 구현
- [ ]  AI 결과 기반 위험 점수 또는 예측 위험도 표시
- [ ]  WebSocket 실시간 알림 안정화 또는 재연결 처리
- [ ]  Discord / Slack Webhook 외부 알림 구현
- [ ]  커스텀 Prometheus 메트릭 추가
- [ ]  Grafana 대시보드에 AI 처리 시간 / 알람 수 / task 처리 시간 포함
- [ ]  k6 등 부하 테스트 결과와 병목 분석 포함
- [ ]  GitHub Actions 기반 CI 또는 CI/CD 구성
- [ ]  Kubernetes Deployment / Service / Ingress 적용
- [ ]  ConfigMap / Secret 기반 환경 설정 분리
- [ ]  HPA 자동 확장 또는 장애 복구 시연
- [ ]  트러블슈팅 사례를 로그 + 메트릭 기반으로 정리

---

## 9. 발표 직전 최종 확인

- [ ]  발표자 순서를 정했다.
- [ ]  각자 어떤 부분을 말할지 정했다.
- [ ]  시연 담당자를 정했다.
- [ ]  시연 중 가장 먼저 보여줄 화면을 정했다.
- [ ]  AI 분석 결과를 재현하는 방법을 정리했다.
- [ ]  위험 발생 장면을 재현하는 방법을 정리했다.
- [ ]  Grafana에서 보여줄 지표를 정했다.
- [ ]  Docker Compose 실행 상태를 확인했다.
- [ ]  발표 전에 한 번 더 수정하거나 점검할 항목을 정리했다.

발표 전에 한 번 더 수정하거나 점검할 항목:

1. 
    
2. 
    
3. 
    

---

## 팀 자가점검 결과

- 팀명: __________________
- 발표자: __________________
- 최종 점검일: __________________

최종 상태:

- [ ]  발표 가능
- [ ]  일부 보완 필요
- [ ]  발표 전 수정 필요

---

## 발표에서 반드시 보여줄 핵심 장면

1. 센서 데이터가 유입되는 장면
    
2. AI 이상 탐지가 실행되고 결과가 저장되는 장면
    
3. Grafana에서 운영 지표를 확인하는 장면
    
4. 위험 발생 시 알람 또는 이벤트가 반영되는 장면
    

---

## 한 줄 최종 점검

```
4차 발표는 화면이 아니라 운영 흐름을 보여주는 발표입니다.
센서 데이터 → AI 분석 → 비동기 처리 → 결과 저장 → 알람 → 모니터링 흐름이 이어지면 발표 가능합니다.
```
