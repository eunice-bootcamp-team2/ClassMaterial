### 발표 가이드
```
※ 이번 발표의 가장 중요한 평가 기준은 기능 개수가 아니라  
데이터 흐름이 끊기지 않고 이어지는지입니다.  
  
센서 데이터 → 저장 → AI 분석 → 결과 저장 → 알람 → 모니터링  
이 흐름이 한 번이라도 완전히 동작해야 합니다.
```

발표의 목적
```
첫째, 3차에서 만든 관제 화면과 데이터 흐름을 실제 운영 구조로 확장했는가
둘째, AI 기반 이상 탐지와 모니터링으로 위험을 사전에 감지할 수 있는가
```

이 프로젝트는 산업 현장에서 발생할 수 있는 유해가스, 전력 이상, 작업자 위치 위험 등을 통합하여 운영자가 한 화면에서 파악하고 대응할 수 있는 관제 플랫폼입니다.

4차 발표의 핵심은 단순히 화면이 보이는 수준을 넘어서, 센서 데이터가 수집되고, Redis를 통해 안정적으로 전달되며, Django 관제 서버에서 저장/판단되고, Celery를 통해 AI 분석이 비동기로 실행되며, Prometheus와 Grafana로 시스템 상태를 확인할 수 있는 **운영 가능한 구조**를 보여주는 것입니다.

전체 전략은 다음처럼 설명할 수 있습니다.

```
1단계 = 보여주는 것 (UI + 데이터 흐름)
2단계 = 살아있는 시스템 (AI + 모니터링 + 운영)
```

따라서 4차 발표의 핵심은 아래 문장으로 정리할 수 있습니다.

```
3차 프로젝트: 보이게 만들어라
4차 프로젝트: 제대로 돌아가게 만들어라
```

---

## 발표에서 가장 중요하게 봐야 하는 기준

이번 발표에서는 아래 5가지를 중심으로 설명하는 것이 좋습니다.

### (1) 이 관제 플랫폼이 무엇을 해결하는지

- 산업 현장에서 어떤 위험을 감지하려는지
- 유해가스, 전력 이상, 작업자 위치 위험이 왜 통합 관리되어야 하는지
- 단순 모니터링이 아니라 사전 감지/예측이 왜 중요한지
- 운영자가 어떤 상황에서 이 시스템을 사용하게 되는지

---

### (2) 3차에서 4차로 무엇이 확장되었는지

3차는 UI와 실시간처럼 보이는 데이터 흐름, 지오펜스 판단, 알람 표시가 핵심이었습니다.

4차는 여기에 다음 구조를 추가합니다.

- 실제 운영 구조
- AI 이상 탐지
- Redis 기반 실시간 처리 안정화
- Celery 비동기 처리
- 알림 고도화
- Docker 기반 서비스 분리
- Prometheus / Grafana 모니터링
- Kubernetes 확장 구조 (선택 또는 설계 수준 설명 가능)

즉, 4차 발표에서는 아래 흐름을 명확히 보여줘야 합니다.

```
센서 데이터 → Redis → Django 저장/판단 → Celery 비동기 작업 → FastAPI AI 분석 → 결과 저장 → 알람/대시보드 출력 → Prometheus/Grafana 모니터링
```

※ Kubernetes는 일부 적용만으로도 충분합니다.

---

### (3) AI 이상 탐지가 실제 서비스 흐름에 연결되는지

AI 기능은 단순히 모델을 실행하는 것이 아니라 관제 서비스 안에서 위험 판단을 보조하는 구조로 동작해야 합니다.

반드시 설명할 내용:

- 어떤 데이터를 AI 분석 대상으로 삼았는지
- ARIMA 또는 이상 탐지 모델이 어떤 역할을 하는지
- 정상 / 이상 판단 결과가 어떻게 저장되는지
- AI 결과가 대시보드 또는 알람 흐름에 어떻게 반영되는지
- AI 결과가 단순 출력이 아니라 DB에 기록되는지

핵심 문장:

```
AI 기능 = 모델 실행이 아니라 위험 판단 흐름에 연결된 분석 결과
```

---

### (4) 비동기 처리와 운영 구조를 이해하고 있는지

AI 분석, 대량 센서 처리, 알림 처리는 요청 즉시 처리하면 서버가 느려질 수 있습니다. 따라서 4차에서는 Celery + Redis 기반 비동기 처리를 보여줘야 합니다.

반드시 설명할 내용:

- Redis의 역할
- Celery의 역할
- Django와 Celery가 어떻게 연결되는지
- 작업 상태가 어떻게 관리되는지
- 실패 시 어떤 상태로 저장되는지

기본 상태값 예시:

```
PENDING     → 요청됨
PROCESSING  → 처리중
SUCCESS     → 완료
FAIL        → 실패
```

핵심 문장:

```
비동기 처리 = 사용자 응답과 무거운 처리를 분리하는 구조
```

---

### (5) 어디가 느린지 / 어디가 문제인지 볼 수 있는지

4차의 중요한 차이는 운영 가능성입니다.

따라서 Prometheus와 Grafana를 통해 아래 지표를 확인할 수 있어야 합니다.

- 요청 수
- 응답 시간
- 에러율
- WebSocket 연결 상태 또는 데이터 처리량
- AI 처리 시간
- Celery task 처리 시간
- 알람 발생 수
- DB 저장 시간 또는 지오펜스 판단 시간

핵심 문장:

```
모니터링의 목적은 단순 그래프가 아니라 병목 지점 확인입니다.
```

---

## 시스템 구조 설명

4차 프로젝트는 다음 구조를 기준으로 설명합니다.

```
FastAPI (센서)
   ↓
Redis (스트림 / PubSub / 브로커)
   ↓
Django + DRF (관제 서버)
   ↓
Celery (비동기 처리)
   ↓
FastAPI (AI 분석)
   ↓
DB 저장 / 알람 처리
   ↓
Prometheus → Grafana
   ↓
Docker Compose / Kubernetes
```

또는 발표에서는 아래처럼 단순화해서 설명해도 됩니다.

```
[센서 데이터 수집] → [실시간 처리 안정화] → [관제 서버 저장/판단]
→ [AI 비동기 분석] → [알람/대시보드 반영] → [운영 모니터링]
```

---

## 기술 스택 역할 설명

기술 이름을 나열하기보다 역할 중심으로 설명합니다.

| 기술 | 역할 |
|---|---|
| FastAPI 센서 서버 | 센서 데이터 수집/생성 및 실시간 전달 |
| Redis | 데이터 버퍼링, Pub/Sub, Celery 브로커 |
| Django + DRF | 관제 서버, 데이터 저장, 권한, API, 이벤트 판단 |
| Celery | AI 분석 및 무거운 작업의 백그라운드 처리 |
| FastAPI AI 서버 | ARIMA 등 AI 이상 탐지 모델 실행 |
| PostgreSQL | 센서 데이터, 이벤트, AI 결과, 작업 상태 저장 |
| WebSocket | 실시간 데이터/알림 전달 |
| Prometheus | Django/FastAPI/Celery 메트릭 수집 |
| Grafana | 운영 대시보드 시각화 |
| Docker Compose | 여러 서비스를 분리하여 실행 |
| Kubernetes | 확장, 장애 복구, 운영 환경 구성 |

---

## 발표에서 반드시 설명해야 하는 핵심 기능

이번 4차 발표에서 가장 중요하게 다뤄야 하는 기능은 아래 7개입니다.

---

### (1) AI 이상 탐지

설명할 내용:

- 센서 시계열 데이터를 분석 대상으로 사용했는지
- ARIMA 또는 이상 탐지 로직을 적용했는지
- 정상 / 이상 구분 결과가 나오는지
- AI 결과가 DB에 저장되는지
- 저장된 결과가 대시보드 또는 알람 흐름에 반영되는지

완료 기준:

- 정상/이상 판단 결과 확인 가능
- AI 분석 결과 DB 저장
- 발표에서 AI 분석 결과를 조회 또는 화면으로 확인 가능

---

### (2) Celery 비동기 처리

설명할 내용:

- 센서 데이터 또는 AI 분석 요청이 Celery task로 분리되는지
- API 응답과 AI 처리가 분리되는지
- 작업 상태가 PENDING / PROCESSING / SUCCESS / FAIL로 관리되는지
- 실패 시 에러 메시지 또는 실패 상태가 저장되는지

완료 기준:

- 사용자 요청 후 즉시 응답 가능
- 백그라운드에서 AI 분석 처리
- task 상태 조회 가능

---

### (3) Redis 도입

설명할 내용:

- Redis가 어떤 역할을 하는지
- Pub/Sub, Stream, Celery broker 중 어떤 용도로 사용했는지
- Redis를 통해 실시간 처리 안정성이 어떻게 좋아졌는지

완료 기준:

- Redis 서비스가 Docker Compose에서 실행됨
- Celery 또는 실시간 데이터 흐름과 연결됨

---

### (4) 모니터링

설명할 내용:

- Prometheus가 어떤 서버의 메트릭을 수집하는지
- Grafana에서 어떤 지표를 보여주는지
- 장애나 병목을 어떤 지표로 확인할 수 있는지

확인 항목:

- 요청 수
- 응답 시간
- 에러율
- AI 처리 시간
- Celery task 처리 시간
- 알람 발생 수

완료 기준:

- Prometheus target 확인 가능
- Grafana 대시보드에서 핵심 지표 확인 가능
- 발표에서 최소 1개 이상의 운영 지표를 설명 가능

---

### (5) 알림 고도화

설명할 내용:

- 위험 발생 시 어떤 방식으로 알림이 전달되는지
- WebSocket 알림 또는 Discord/Slack Webhook을 사용했는지
- 알림 이력이 DB에 저장되는지
- 동일 알람이 과도하게 반복되지 않도록 처리했는지

완료 기준:

- 위험 발생 시 알림 확인 가능
- 알림 또는 이벤트 이력 확인 가능

---

### (6) Docker 구성

설명할 내용:

- 서비스가 어떻게 컨테이너로 분리되어 있는지
- Django, FastAPI, Redis, PostgreSQL, Celery, Prometheus, Grafana가 어떻게 실행되는지
- `.env` 또는 환경변수 관리가 되어 있는지

기본 구성 예시:

```
django / fastapi-sensor / fastapi-ai / redis / postgres / celery / prometheus / grafana
```

완료 기준:

- Docker Compose로 주요 서비스 실행 가능
- README에 실행 방법 정리
- 팀원이 아닌 사람도 실행 흐름을 이해할 수 있음

---

### (7) Kubernetes 또는 운영 확장 구조

설명할 내용:

- Kubernetes를 실제 적용했는지, 또는 확장 설계로 정리했는지
- Deployment, Service, Ingress, HPA의 역할을 설명할 수 있는지
- ConfigMap / Secret으로 설정을 분리했는지

완료 기준:

- 실제 구현 시: Pod, Service, Ingress 동작 확인 가능
- 미구현 시: Docker Compose 기반 운영 구조와 향후 K8s 확장 계획 설명 가능

---

## 발표에서 반드시 포함해야 하는 자료

필수 자료:

- 프로젝트 개요 1장
- 3차 → 4차 확장 구조 설명 1장
- 전체 시스템 아키텍처 구조도
- 데이터 흐름도
- AI 분석 흐름도
- 비동기 처리 흐름도
- Docker Compose 서비스 구성도
- Prometheus / Grafana 화면
- 핵심 시연 화면
- 장애 또는 병목 확인 방식 설명 자료

있으면 좋은 자료:

- ERD 또는 확장 ERD
- task 상태 테이블 예시
- AI 분석 결과 테이블 예시
- Prometheus 주요 PromQL 예시
- Docker Compose 실행 명령어
- Kubernetes 구조도
- 트러블슈팅 사례 1개

---

## 10분 발표 기준

- 1분: 프로젝트 소개 및 3차 → 4차 확장 방향
- 2분: 시스템 구조 설명 (Django / FastAPI / Redis / Celery / AI / 모니터링)
- 3분: 핵심 시연 (센서 데이터 → AI 분석 → 결과 저장 → 알람/대시보드)
- 2분: Prometheus / Grafana 운영 지표 설명
- 1분: Docker / 배포 구조 설명
- 1분: 한계점, 트러블슈팅, 향후 개선 방향

---

# 평가 기준

## 채점 기본 원칙

- 필수 항목은 기본점수 100점 기준으로 채점합니다.
- 선택 기능 또는 추가 구현은 가산점 항목으로 처리합니다.
- 단, 필수 흐름이 부족한 경우 가산점이 기본점수를 대체할 수 없습니다.
- 4차 발표는 단순 화면 시연이 아니라 **AI + 비동기 + 운영 + 모니터링 구조가 실제로 연결되었는지**를 중심으로 평가합니다.
- 기능 개수보다 데이터 흐름, 운영 가능성, 장애 추적 가능성을 더 중요하게 봅니다.

---

## A. 기본점수 평가표 (100점)

| 평가 영역                  | 세부 평가 기준                                                                        |  배점 |
| ---------------------- | ------------------------------------------------------------------------------- | --: |
| 1. 프로젝트 소개 및 4차 범위 이해도 | 관제 플랫폼의 필요성, 3차와 4차의 차이, 4차 목표를 명확히 설명하는가                                       |  10 |
| 2. 시스템 아키텍처 설명         | FastAPI 센서 서버, Redis, Django, Celery, FastAPI AI 서버, DB, 모니터링 구조를 역할 중심으로 설명하는가 |  15 |
| 3. AI 이상 탐지 구현         | AI 분석이 실제 데이터 흐름에 연결되어 결과가 DB 저장 → 알람 또는 대시보드까지 반영되는가                           |  15 |
| 4. 비동기 처리 구조           | Celery + Redis 기반으로 AI 분석 또는 무거운 작업을 분리하고 task 상태를 관리하는가                        |  15 |
| 5. 핵심 데이터 흐름 완성도       | 센서 데이터 → 저장 → AI 분석 → 결과 저장 → 알람 → 모니터링까지 하나의 흐름으로 실제 동작하는가                     |  15 |
| 6. 운영 모니터링             | Prometheus/Grafana를 통해 요청 수, 응답시간, 에러율, AI 처리 시간 등 지표를 확인하고 병목을 설명할 수 있는가       |  15 |
| 7. Docker / 배포 구조      | Docker Compose 기반으로 Django, FastAPI, Redis, PostgreSQL 등 서비스가 분리 실행되는가          |   5 |
| 8. 발표 자료 및 문서화         | 구조도, 데이터 흐름도, 실행 방법, API/Task 상태, 모니터링 자료가 준비되어 있는가                             |   5 |
| 9. 실행 가능성 / 안정성        | 발표 중 핵심 흐름이 오류 없이 동작하고, 실패 시 원인을 설명할 수 있는가                                      |   5 |
| 합계                     |                                                                                 | 100 |

---

## B. 핵심 기능 세부 채점 기준 (40점 상세)

※ 아래 항목은 A 기본점수 평가를 세부적으로 판단하기 위한 참고 기준입니다.  
※ 별도의 점수로 합산하지 않으며, A 항목 채점 시 기준으로 사용합니다.

| 핵심 기능 항목          | 평가 기준                                                           |  배점 |
| ----------------- | --------------------------------------------------------------- | --: |
| AI 이상 탐지          | 이상 탐지 로직을 적용하고 결과가 DB 저장 및 알람/대시보드 흐름에 연결되는가                    |   7 |
| AI 결과 저장          | AI 분석 결과가 단순 응답이 아니라 DB에 저장되고 조회 가능한가                           |   5 |
| Celery 비동기 처리     | AI 분석 또는 센서 처리 작업이 Celery task로 분리되어 실행되는가                      |   6 |
| Task 상태 관리        | PENDING / PROCESSING / SUCCESS / FAIL 상태 관리와 상태 조회가 가능한가        |   5 |
| Redis 연결          | Redis가 Celery broker 또는 실시간 처리 안정화 용도로 실제 연결되어 있는가              |   4 |
| 알림 고도화            | 위험 발생 시 WebSocket 또는 외부 알림으로 결과가 전달되는가                          |   4 |
| Docker Compose 구성 | 주요 서비스가 컨테이너로 분리되어 실행되는가                                        |   5 |
| 모니터링 지표           | Prometheus / Grafana에서 요청 수, 응답시간, 에러율 등을 확인하고 병목 지점을 설명할 수 있는가 |   4 |
| 합계                |                                                                 |  40 |

---

## C. 발표 감점 기준

| 감점 항목                                         |       감점 |
| --------------------------------------------- | -------: |
| 실행 불가 상태로 발표 진행                               |      -10 |
| 핵심 시연 중 오류로 데이터 흐름 확인 불가                      | -5 ~ -10 |
| AI 분석 결과가 DB에 저장되지 않거나 결과 흐름이 연결되지 않음         |       -5 |
| Celery / Redis 구조를 설명하지 못함                    |  -3 ~ -5 |
| 모니터링 화면만 있고 지표 의미를 설명하지 못함                    |       -3 |
| Docker Compose 실행 구조가 불명확함                    |       -3 |
| 실제 구현하지 않은 Kubernetes / AI / 모니터링을 구현한 것처럼 발표 | -5 ~ -10 |
| 발표가 3차 수준의 UI 시연에만 머무름                        |       -5 |
| 역할 설명 없이 기술 이름만 나열                            |       -3 |
| 발표 시간 초과 또는 발표 흐름 붕괴                          |  -2 ~ -5 |
| Celery worker 또는 Redis 연결 문제로 비동기 흐름이 동작하지 않음 |       -5 |


---

## D. 가산점 항목 (최대 +20점)

| 선택 항목 | 가산점 |
|---|--:|
| ARIMA 외 Isolation Forest, Z-score, Change Point 등 이상 탐지 조합 구현 | +3 |
| AI 결과 기반 위험 점수 또는 예측 위험도 표시 | +3 |
| WebSocket 실시간 알림 안정화 또는 재연결 처리 | +2 |
| Discord / Slack Webhook 외부 알림 구현 | +2 |
| 커스텀 Prometheus 메트릭 추가 | +3 |
| Grafana 대시보드에 AI 처리 시간 / 알람 수 / task 처리 시간 포함 | +3 |
| k6 등 부하 테스트 결과와 병목 분석 포함 | +3 |
| GitHub Actions 기반 CI 또는 CI/CD 구성 | +3 |
| Kubernetes Deployment / Service / Ingress 적용 | +4 |
| ConfigMap / Secret 기반 환경 설정 분리 | +2 |
| HPA 자동 확장 또는 장애 복구 시연 | +3 |
| 트러블슈팅 사례를 로그 + 메트릭 기반으로 정리 | +2 |

가산점 적용 규칙:

- 가산점은 구현 + 시연 + 설명이 모두 확인된 경우만 인정합니다.
- 코드만 존재하거나 발표에서 보여주지 못하면 인정하지 않습니다.
- 가산점 총합은 최대 +20점까지만 반영합니다.
- 필수 구조가 부족한 경우 가산점이 기본점수를 대체하지 않습니다.

---

## E. 최종 점수 계산 방식

```
기본점수: 100점 만점
가산점: 최대 +20점 별도 표기
최종 표기: 기본점수 / 100, 가산점 / +20
```

예시:

```
기본점수 86 / 100
가산점 +12
총평: AI + 비동기 + 모니터링 구조가 안정적으로 연결됨
```

---

## F. 채점 총평 예시 기준

| 구간 | 평가 |
|---|---|
| 90점 이상 | AI, 비동기, 운영 모니터링 구조가 매우 안정적으로 구현됨 |
| 80~89점 | 핵심 구조는 완성되었고 일부 운영/설명 보완 필요 |
| 70~79점 | 기본 구현은 되었으나 AI/비동기/모니터링 중 일부 연결 보완 필요 |
| 60~69점 | 일부 핵심 기능은 있으나 운영 가능한 구조로 보기에는 부족함 |
| 59점 이하 | 4차 필수 흐름인 AI + 비동기 + 운영 구조 확인이 어려움 |

---

## G. 한 줄 채점 기준 정리

```
4차 발표는 화면이 보이는가가 아니라,
AI 분석 + 비동기 처리 + 모니터링 + 배포 구조가 실제 운영 흐름으로 연결되었는가를 평가합니다.
```


※ 평가 시 가장 우선적으로 확인하는 것은 “데이터 흐름 시연”입니다.  
각 기능이 개별적으로 존재하더라도, 하나의 흐름으로 연결되지 않으면 높은 점수를 받을 수 없습니다.