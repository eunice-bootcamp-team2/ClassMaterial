### 전체 학습 순서
| 순서     | 구분          | 사고 방식          | 탐지 방식             |
| ------ | ----------- | -------------- | ----------------- |
| STEP 1 | 통계 기반       | 수학적 규칙 / 분포 가정 | Z-score / Rolling |
| STEP 2 | 실시간 처리 구조   | 데이터 자르는 전략     | Sliding Window    |
| STEP 3 | ML 기반 이상 탐지 | 머신러닝 모델        | Isolation Forest  |
| STEP 4 | 상태 전이 탐지    | 시계열 구조 변화 탐지   | Change Point      |

개념 흐름
```
[센서 데이터 입력]
        ↓
┌──────────────────────────────┐
│ STEP 1 — 통계 기반 계산        │
│ 평균 / 표준편차 / Z-score      │
└──────────────────────────────┘
        ↓
┌──────────────────────────────┐
│ STEP 2 — 실시간 처리 구조      │
│ Sliding Window / 상태 머신    │
└──────────────────────────────┘
        ↓
┌──────────────────────────────┐
│ STEP 3 — ML 기반 탐지         │
│ Isolation Forest / 패턴 학습  │
└──────────────────────────────┘
        ↓
┌──────────────────────────────┐
│ STEP 4 — 상태 변화 탐지        │
│ Change Point / 구조 변화 감지  │
└──────────────────────────────┘
```

핵심 포함 관계 이해

실제 관제 시스템에서는:
✔ STEP 1 계산 로직  
✔ STEP 2 실시간 구조  
✔ STEP 3 ML 모델  
✔ STEP 4 상태 변화 탐지
전부 동시에 존재하는 경우가 많습니다. 왜냐하면 탐지 목적이 다르기 때문입니다.

###### 각 단계 역할
| 단계     | 실제 역할        |
| ------ | ------------ |
| STEP 1 | 값이 튀었는지 계산   |
| STEP 2 | 실시간으로 처리     |
| STEP 3 | 복잡한 패턴 탐지    |
| STEP 4 | 상태가 바뀌었는지 탐지 |

관제 플랫폼 설계 시 실제 적용 흐름
```
센서 데이터 수신  
↓  
Sliding Window 유지 (STEP 2)  
↓  
통계 계산 (STEP 1)  
↓  
ML 모델 판단 (STEP 3)  
↓  
상태 변화 감지 (STEP 4)  
↓  
알람 / 이벤트 / 로그 / 대시보드
```

센서 특성:

✔ 계속 들어옴 (스트리밍)  
✔ 라벨 없음 (정답 데이터 없음)  
✔ 즉시 판단 필요  
✔ 계산 빨라야 함  
✔ 설명 가능해야 함

### 통계 기반 이상 탐지 (Statistical Anomaly Detection)란?
	데이터의 분포 특성(평균, 분산, 표준편차 등)을 이용하여 정상 범위를 벗어난 값을 
	비정상(이상치)으로 판단하는 규칙 기반 탐지 방식입니다.
	즉, 쉽게 설명하면 평소 값의 범위를 통계로 계산하고, 그 범위를 벗어나면 이상으로 간주하는 
	방법입니다.

핵심 개념을 포함한 실무형 정의

통계 기반 이상 탐지는:
✔ 데이터의 정상 상태 패턴을 통계량으로 표현하고  
✔ 현재 값이 그 패턴에서 얼마나 벗어났는지를 계산하여  
✔ 사전에 정의된 기준(임계치)으로 이상 여부를 판단하는 방식입니다.

판단 기준:
- 평균 (Mean)
- 표준편차 (Standard Deviation)
- 분산 (Variance)
- 분위수 (Quantile)
- 확률 분포 즉, 확률 / 분포 / 통계량 기반 판단합니다.

이상치 판단의 기본 철학:
정상 데이터는 일정한 범위 안에 존재한다
그래서 
✔ 정상 범위 계산  
✔ 범위 밖 → 이상

통계 기반 이상 탐지에서 자주 쓰는 방법:
- Z-score 기반 탐지
- Rolling Mean / Std 기반 탐지
- IQR (사분위 범위)
- 임계값(Threshold) 규칙
- 확률 밀도 기반 판단

관제 시스템 / 센서 데이터 기준:
> 실시간으로 유입되는 데이터의 최근 통계 특성을 기준으로  
> 비정상적으로 벗어난 신호를 즉시 감지하는 경량 탐지 방식

###### 머신러닝 기반 이상 탐지와의 근본 차이
| 구분      | 통계 기반    | ML 기반     |
| ------- | -------- | --------- |
| 판단 기준   | 통계량 / 규칙 | 학습된 모델    |
| 학습 필요   | 없음       | 필요        |
| 해석      | 매우 쉬움    | 상대적으로 어려움 |
| 실시간 처리  | 매우 유리    | 상황에 따라 부담 |
| 데이터 요구량 | 적음       | 많음        |
통계 기반 이상 탐지는 본질적으로 머신러닝모델이나 학습기반이 아닌 
"공식 + 규칙 + 계산로직" 입니다.
가장 정확한 표현으로는 규칙 기반 탐지 알고리즘 (Rule-Based Detection Logic)입니다.

머신러닝 모델처럼 `fit()` 하지 않지만, 운영 시스템에서는 실시간 판단 엔진 역할을 합니다.

그래서 이 파트는 통계 기반 탐지로 이런 위치에서 사용됩니다.
```
[실시간 시스템 / 관제 시스템 / 모니터링 시스템]
```
대표 사례:

✔ 센서 이상 탐지  
✔ 서버 장애 탐지  
✔ 트래픽 급증 감지  
✔ 금융 거래 이상 감지  
✔ 제조 설비 모니터링

어떤 코드 구조와 연결해서 사용하나?
이건 “모델 안에 넣는 코드”가 아니라 판단 로직 / 필터 / 알람 엔진 형태로 사용합니다.
실제 구조:
```
데이터 수집(실시간 센서데이터)
→ 통계 계산 (Rolling mean/std)
→ 이상 판단 (Z-score)
→ 알람 발생
```
	즉 모델이 아니라 시스템 로직의 일부입니다.

---
패턴 뼈대코드 (Rolling mean/std → z-score → 임계치 판단 알람)
```python
# ============================================
# [STEP 1 Skeleton] 통계 기반 이상 탐지 패턴
# Rolling Mean / Rolling Std / Z-score / Alert
# ============================================

import numpy as np
import pandas as pd

# (0) 데이터 준비
# df: columns = ["timestamp", "value"]
df = df.sort_values("timestamp").reset_index(drop=True)

# (1) Rolling window 설정 (예: 최근 30개)
WINDOW = 30

# (2) Rolling 통계 계산
df["roll_mean"] = df["value"].rolling(window=WINDOW, min_periods=WINDOW).mean()
df["roll_std"]  = df["value"].rolling(window=WINDOW, min_periods=WINDOW).std(ddof=0)

# (3) Z-score 계산 (표준편차가 0인 경우 방지)
EPS = 1e-9
df["z_score"] = (df["value"] - df["roll_mean"]) / (df["roll_std"] + EPS)

# (4) 임계치(Alert) 규칙
Z_THRESHOLD = 3.0
df["is_anomaly"] = df["z_score"].abs() >= Z_THRESHOLD

# (5) 결과 확인
# - 최근 구간에서 알람이 언제 울렸는지 확인
anomalies = df[df["is_anomaly"]]
print(anomalies[["timestamp", "value", "roll_mean", "roll_std", "z_score"]].tail(10))
```

해석:
`sort_values("timestamp")` : 시간순으로 정렬
- `df` 안에는 `timestamp`(시간)과 `value`(센서값)가 있으면
- 이상탐지(rolling mean/std, z-score)는 “이전 값들(최근 30개)”을 기준으로 계산합니다.
- 그런데 데이터가 시간순이 아니라 섞여 있으면?

###### 예를 들어 데이터가 이렇게 섞여 있으면:
|timestamp|value|
|---|---|
|10:00:03|101|
|10:00:01|99|
|10:00:02|100|
무조건 시간 기준 오름차순 정렬을 해줍니다.

`reset_index(drop=True)` : 인덱스를 0부터 다시 깔끔하게 재설정
만약 오름차순 정렬한 인덱스가 그대로 남아있다면 인덱스가 꼬일수 있으므로 다시 정렬합니다.
예시: 정렬전 0,1,2,3... 정렬후 2,3,1...  이항태가 되면 디버깅시도 헷갈리고 혼란이 있으므로 정렬된 기준으로 다시 인덱스를 재설정 합니다.

즉, 이단계는 시계열 데이터가 ‘진짜 시간 순서’대로 처리되도록 정렬하고, 이후 작업이 헷갈리지 않도록 인덱스를 깔끔하게 다시 매기는 초기 준비 단계입니다.

---
Rolling window 설정이란?
최근 데이터 몇 개를 기준으로 계산할지 정하는 것으로 센서 데이터는 계속 변합니다.
```
과거 전체 평균 → 의미 없음
지금 상태 기준 → 중요
```
그래서 우리는 이런 질문을 합니다. 최근 값들 기준으로 보면 정상인가? 
이때 사용하는 것이 Rolling Window입니다.

Rolling Window를 그림으로 이해
WINDOW = 3 이라고 가정

데이터
```
[10, 11, 9, 10, 50]
```

Rolling 계산 방식:
```
(10,11,9) → 평균 계산
(11,9,10) → 평균 계산
(9,10,50) → 평균 계산
```
✔ 창(window)이 옆으로 이동하면서 계산  
✔ 항상 “최근 N개”만 봄

(1) Rolling window 설정
예시: 센서가 1초마다 들어오면:
```
WINDOW = 30 → 최근 30초 의미
```

왜 전체 평균 안 쓰고 Rolling 쓰나?

전체 평균 문제:
❌ 과거 오래된 값 영향 큼  
❌ 상태 변화 반영 느림  
❌ 실시간 시스템에 부적합

Rolling 평균:
✅ 최근 상태만 반영  
✅ 시스템 현재 상태 추적 가능  
✅ 이상 탐지에 적합

관제 시스템 관점에서 해석
Rolling Window는 사실상 “현재 정상 상태 기준선을 만드는 장치” 입니다.
즉, 최근 데이터 묶음 → 정상 패턴 추정 하여 그 기준으로 이상 판단을 합니다.

---
(2) Rolling 통계 계산
```python
df["roll_mean"] = df["value"].rolling(window=WINDOW, min_periods=WINDOW).mean()
df["roll_std"]  = df["value"].rolling(window=WINDOW, min_periods=WINDOW).std(ddof=0)
```
	최근 WINDOW 구간의 정상 기준을 만드는 코드

이 코드가 하는 일은 각 시점마다 최근 WINDOW개 값의 평균(roll_mean)과 표준편차(roll_std)를 계산해서, 현재 값이 정상 범위인지 판단할 기준을 만듦니다.

`df["value"].rolling(...)` : `rolling`은 “움직이는 창(window)” 을 뜻해요
- `window=WINDOW` → 창의 크기(예: 30개)
- 창이 한 칸씩 앞으로 이동하면서,
- 그 창 안에 있는 값들로 통계를 계산합니다.

위에서 설명한 대로 예를 들어 WINDOW=3이라면:
value가 `[10, 11, 9, 10, 50]`일 때
- 첫 창: `[10, 11, 9]`
- 다음 창: `[11, 9, 10]`
- 다음 창: `[9, 10, 50]`
    
이렇게 “최근 3개씩” 묶어서 계산해요.

`mean()` : Rolling Mean (최근 평균)
```python
...rolling(...).mean()
```
	지금 시점 기준으로 최근 WINDOW개 값의 평균

`std(ddof=0)` : Rolling Std (최근 표준편차)
```python
...rolling(...).std(ddof=0)
```

표준편차란 흔들림 정도(변동성)입니다.

각 시점에서의 의미: 최근 WINDOW개 값이 평균 주변에서 얼마나 흔들리는지
- 흔들림이 작으면 std가 작음 → 작은 변화도 이상으로 볼 수 있음
- 흔들림이 크면 std가 큼 → 변화에 더 관대해짐
그래서 Z-score가 가능합니다. 평소에 많이 흔들리는 센서와 거의 안 흔들리는 센서를 공정하게 판단합니다.

---
(3) Z-score 계산 : (표준편차가 0인 경우 방지)
```python
EPS = 1e-9  
df["z_score"] = (df["value"] - df["roll_mean"]) / (df["roll_std"] + EPS)
```

`1e-9` 가 뜻하는 값
- 과학적 표기법 (Scientific Notation)로 `e` = exponent (지수)와 `-9` = 10의 -9승으로 표기하며
- `1e-9` =0.000000001 (10의 -9승)
- 아주아주 작은 숫자(거의 0에 가까움)

위의 계산은 표준편차(roll_std)가 0이 되는 순간, 나눗셈이 터지는 걸 막는 안전장치입니다
- 분모가 0이면 `(어떤 값) / 0` 이 되어 계산이 불가능(에러/무한대)이 됨
- 그래서 `roll_std`에 아주 작은 수(EPS)를 더해서 분모가 0이 되는 상황을 피함
- EPS는 수의 범위가 아니라
- 0으로 나누는 오류 방지용 상수(안전핀)입니다.
- 목적은 거의 0인데 계산에는 문제 안 생기게 하는 값입니다.

---
(4) 임계치(Alert) 규칙
```python
Z_THRESHOLD = 3.0
df["is_anomaly"] = df["z_score"].abs() >= Z_THRESHOLD
```
정상 법위의 기준을 3으로 두고 

`df["z_score"]` : 현재 값이 평균에서 얼마나 튀었는지의 점수
`.abs()` : 절대값 함수로 (+4)위로 튀어도 이상, (-4)아래로 튀어도 이상이므로 부호제거
`>= Z_THRESHOLD` : 이 정도 이상이면 경보 울려라 
- threshold = 3
- z_score = 2.5 → 정상
- z_score = 3.2 → 이상

---
(5) 결과 확인 - 최근 구간에서 알람이 언제 울렸는지 확인
```python
anomalies = df[df["is_anomaly"]]
print(anomalies[["timestamp", "value", "roll_mean", "roll_std", "z_score"]].tail(10))
```
이 구간에서 이상수치 구간만 필터하여 끝에서 10개 데이터만 리스트에 있는 데이터 기준으로 출력합니다.

---
### 실무를 위한 연습 전체코드
```python
import numpy as np
import pandas as pd
import plotly.express as px

# -------------------------------------------------
# (0) 데이터 준비 (실습용 센서 데이터 생성)
#    - df: columns = ["timestamp", "value"]
# -------------------------------------------------
np.random.seed(42)

n = 600  # 10분치(예: 1초 간격 600개)
timestamps = pd.date_range("2026-02-11 10:00:00", periods=n, freq="s")

# 정상 구간: 평균 100, 노이즈 약간
values = 100 + np.random.normal(0, 1.2, size=n)

# 이상치(스파이크) 일부 삽입
spike_idx = [120, 121, 260, 400, 520]
values[spike_idx] += [12, -14, 18, -20, 15]

df = pd.DataFrame({"timestamp": timestamps, "value": values})

# (실시간 데이터처럼 쓰려면, 항상 시간순 정렬이 기본)
df = df.sort_values("timestamp").reset_index(drop=True)


# -------------------------------------------------
# (1) Rolling window 설정 (예: 최근 30개 = 최근 30초)
# -------------------------------------------------
WINDOW = 30          # 최근 30초(1초 간격이면 30개)
Z_THRESHOLD = 3.0    # 보통 3σ 규칙(상황에 따라 2.5~4 조절)

# -------------------------------------------------
# (2) Rolling 통계 계산 (rolling mean / rolling std)
#    - roll_mean: 최근 WINDOW 구간의 평균
#    - roll_std : 최근 WINDOW 구간의 표준편차(변동성)
# -------------------------------------------------
# min_periods=WINDOW:
# - WINDOW개 미만일 때는 통계를 계산하지 않음(NaN)
# - 초반 구간은 통계가 불안정하므로 알람을 안 울리게 하는 안전장치
df["roll_mean"] = df["value"].rolling(window=WINDOW, min_periods=WINDOW).mean()
df["roll_std"]  = df["value"].rolling(window=WINDOW, min_periods=WINDOW).std(ddof=0)

# -------------------------------------------------
# (3) Z-score 계산
#    - z_score = (현재값 - 최근평균) / 최근표준편차
#    - "현재 값이 평균에서 표준편차 몇 배나 떨어져 있는가?"를 수치로 표현
# -------------------------------------------------
# 표준편차가 0에 가까우면 분모가 0이 되어 계산이 터질 수 있으므로 작은 값(EPS) 더함
EPS = 1e-9
df["z_score"] = (df["value"] - df["roll_mean"]) / (df["roll_std"] + EPS)

# -------------------------------------------------
# (4) 임계치(Alert) 규칙
#    - abs(z_score) >= Z_THRESHOLD 이면 이상치(경보)로 표시
# -------------------------------------------------
df["is_anomaly"] = df["z_score"].abs() >= Z_THRESHOLD

# -------------------------------------------------
# (5) 결과 확인 (알람 요약 + 알람 발생 시점 출력)
# -------------------------------------------------
alarm_count = int(df["is_anomaly"].sum())
print(f"알람 개수: {alarm_count}개 (Z_THRESHOLD={Z_THRESHOLD}, WINDOW={WINDOW})")

# 알람이 발생한 행만 따로 확인 (언제/얼마나 튀었는지)
alarm_rows = df[df["is_anomaly"]].copy()
print(alarm_rows[["timestamp", "value", "roll_mean", "roll_std", "z_score"]].head(10))

# -------------------------------------------------
# (선택) 시각화 1) 값(value) + 롤링 평균(roll_mean)
#    - 정상 구간에서는 value가 roll_mean 주변에서 움직임
#    - 스파이크가 있으면 선이 갑자기 튀는 모습이 보임
# -------------------------------------------------
fig1 = px.line(df, x="timestamp", y=["value", "roll_mean"], title="Sensor value vs Rolling Mean")
fig1.show()

# -------------------------------------------------
# (선택) 시각화 2) Z-score 그래프
#    - 임계치(Z_THRESHOLD)를 넘는 구간이 이상 신호 후보
# -------------------------------------------------
fig2 = px.line(df, x="timestamp", y="z_score", title="Z-score over time")
fig2.show()

# -------------------------------------------------
# (선택) 시각화 3) 알람 포인트(이상치)만 점으로 표시
#    - 실제 경보가 울리는 시점만 콕 찍어서 확인
# -------------------------------------------------
alarm_plot = df[df["is_anomaly"] & df["roll_mean"].notna()].copy()
fig3 = px.scatter(alarm_plot, x="timestamp", y="value", title="Anomaly points (Threshold Alert)")
fig3.show()
```

---
### 이 코드는 어떤 상황에서 쓰이나?
	현실 상황을 시뮬레이션 해서 설명하겠습니다.

- 공장 설비, 서버 트래픽, 온도/압력 센서, 결제 트랜잭션, IoT 기기 등에서
- 값이 1초마다 계속 들어온다 (스트리밍/실시간 데이터)
- 대부분은 “정상 범위”에서 조금씩 흔들리는데
- 가끔 갑자기 확 튀는 값(스파이크)이 나온다
    - 센서 오작동
    - 기계 이상 징후
    - 네트워크 폭주
    - 장애/이상 이벤트 시작

✅ 그래서 목표는 "최근 30초 기준으로 볼 때, 지금 값이 너무 튀면 바로 이상으로 표시하고 알람을 띄우자" 이것입니다.

---
### 이 코드를 통해 무엇을 증명하거나 얻는가”?

✅ 얻는 것 1: 이상 탐지의 최소 자동화
- “누가 눈으로 보는 것”이 아니라
- 알고리즘이 자동으로 이상 시점을 표시한다
    
✅ 얻는 것 2: 이상 여부를 ‘숫자(z_score)’로 설명 가능
- “그냥 이상해 보여요”가 아니라
- “평균에서 4.8σ(시그마) 튀었습니다”라고 말할 수 있음  
    → 팀 발표/로그/리포트에 강력함

`σ` = 표준편차로 데이터가 평균 주변에서 얼마나 퍼져 있는가(흔들림의 크기)를 나타내는 대표적인 지표입니다.
- `1σ` → 평균에서 표준편차 1배 거리
- `2σ` → 평균에서 표준편차 2배 거리
- `3σ` → 흔히 쓰는 이상치 기준
- `4.8σ` → 엄청 크게 튄 값 (강한 이상 신호)

✅ 얻는 것 3: 튜닝 포인트가 명확해짐
- WINDOW(최근 몇 초를 정상으로 볼지)
- THRESHOLD(얼마나 튀면 알람인지)
    
즉, 기준을 조절해서 민감도를 조절할 수 있는 틀을 얻습니다.

### 어디에 활용할 수 있나?

실무 활용 예시
- 서버/트래픽 모니터링: 요청 수, 응답시간이 갑자기 튀면 경보
- 결제/금융: 거래 금액/빈도가 갑자기 튀면 이상 거래 후보
- 공장 설비: 온도/진동/압력이 갑자기 튀면 기계 이상 조짐
- IoT 관제: 센서 값이 갑자기 튀면 사고 위험 알림
    
시스템에 붙이는 방식(현업)
- 이 로직을 “실시간 스트림”에 붙임
    - Kafka/Redis Stream/웹소켓/DB polling 등
- `is_anomaly=True`면
    - Slack/Discord/Webhook 알림
    - DB에 이벤트 기록
    - Grafana/대시보드에 표시

---
### 전체코드 해석

실시간 센서가 들어온다고 가정하고 데이터를 만든다
- 원래는 센서/로그 시스템에서 값이 들어오지만,
- 지금은 실습용으로 정상 값 + 노이즈를 만들고,
- 일부 시점에 스파이크(이상치)를 의도적으로 넣어.
- 그리고 시간 순서대로 정렬합니다.
    
의미: 현실에서 들어오는 데이터가 이럴 거니까, 테스트용으로 똑같이 만들어보자

(0) 데이터 준비 (실습용 센서 데이터 생성)
```python
np.random.seed(42)
```
	난수(랜덤 값) 생성 결과를 항상 똑같이 나오게 고정하는 설정
원래 랜덤 함수는 실행할 때마다 값이 달라집니다.
실행할 때마다 다른 숫자 생성됨 → 실습/테스트/디버깅 불편합니다.
그래서 seed를 지정하면 매번 실행해도 동일한 랜덤 값이 생성됩니다.
42는 의미없는 숫자로 아무 숫자나 사용해도 됩니다. 관례적으로 42를 자주 씁니다.

---
```python
n = 600  # 10분치(예: 1초 간격 600개)
timestamps = pd.date_range("2026-02-11 10:00:00", periods=n, freq="s")
```
`n = 600`은 임시 테스트 숫자입니다.
- `n` = 생성할 데이터 개수
- 1초 간격 데이터라면 600초 = 10분
즉,
- 1초마다 값이 들어온다고 가정
- 10분 동안 들어온 데이터 개수를 설정합니다.

`pd.date_range(...)`는 함수로 연속된 시간(날짜) 목록을 자동 생성하는 Pandas 함수입니다. 
형태는 다음과 같습니다. 
```
pd.date_range(시작시점, 몇 개 만들지, 간격)
```

아래 부분은 Pandas 함수가 미리 정해둔 파라미터 이름(옵션 이름)으로 바꿀수 없는 이름입니다.
✔ 파라미터 이름 영역 (API 인터페이스)
- periods : 몇 개의 시간을 만들 것인가? `ex) periods=600`
- freq : 시간 간격 규칙 `ex) freq="5S" # 5초`
- start : 시간 생성 시작 시점 `ex) start="2026-02-11 10:00:00"`
- end : 시간 생성 종료 시점 `ex) end="2026-02-11 10:10:00"`
- tz : 타임존(Time Zone) 지정 `ex) tz="Asia/Seoul"`
    
→ 함수 옵션 이름

✔ 규칙 값 영역 (Pandas 규약)
- "s" (초)   # ✅ 소문자
- "H" (시간)
- "D" (일)
- "5S" (5초)
- "10min" (10분)  # ✅ 소문자
    
→ Pandas가 이해하는 약속된 코드

---
정상처럼 보이는 가짜 센서 데이터를 만들기 위한 생성 규칙으로 실험을 위한 데이터 시뮬레이션 도구로 정규 분포에서 가장 많이 쓰이는 근사 모델입니다.

```python
# 정상 구간: 평균 100, 노이즈 약간
values = 100 + np.random.normal(0, 1.2, size=n)
```
	평균 100 주변에서 살짝 흔들리는 정상 센서 값 생성
	100 근처에서 살짝 흔들리는 숫자 n개 만들어줘를 명령하는 코드입니다.

위의 코드는 가짜 센서 값 생성기입니다.
위의 값을 돌리면 랜덤하게 평균중심 아래와 같은 숫자가 나옵니다.
```
+0.5  
-1.3  
+0.8  
-0.2  
+2.1  
-0.9
```
✔ 양수 나옴  
✔ 음수 나옴  
✔ 대부분 0 근처

`np.random.normal(0, 1.2, size=n)` : 이 함수의 의미는 정규분포(가우시안 분포)를 따르는 랜덤 값 생성기

파라미터 의미:
- `0` → 평균(mean)
- `1.2` → 표준편차(std)
- `size=n` → n개 생성

현실에서는 케이스마다 값이 다르게 나옵니다.
- 온도(섭씨) → 음수 가능
- 전압 → 보통 음수 거의 없음
- CPU 사용률 → 절대 음수 불가
- 압력 → 음수 거의 없음
- 로그 트래픽 → 음수 불가
```
values = 기준값(정상 상태) + 랜덤 흔들림
```
	100 근처로 맞추는 이유는 그냥 읽기 편하게 하기 위함입니다.

현실 데이터를 생각하며 이해를 해봅니다.
센서든 시스템이든 대부분 이런 구조입니다.
- 어떤 안정적인 정상 범위가 존재
- 값은 그 주변에서 조금씩 흔들림
- 문제가 생기면 갑자기 튐

예:
- 온도 센서 정상 → 24 ~ 26℃
- 서버 응답시간 정상 → 110 ~ 130ms
- 전류 정상 → 8 ~ 9A
    
➡ 항상 “중심값(평균)”이 존재합니다.
100은 그냥 ‘정상 중심값’을 표현한 것입니다. 100이라는 숫자에는 특별한 의미가 없어요.
✔ 보기 쉽고  
✔ 계산 해석이 직관적이고  
✔ 그래프에서 변화가 잘 보임

온도를 예시로 설명하면
1️⃣ 온도 센서 정상 → 24 ~ 26℃ 평균은 약 25 이것을 100 기준으로 옮기면
```
99 ~ 101
```
이걸 수학적으로 보면:
- 평균 = 25
- 변동 폭 = ±1
변동폭은 온도센서 정상 범위를 기준으로 아래와 같기때문에 ±1 이렇게 나옵니다.
- 25 - 1 = 24
- 25 + 1 = 26

중심을 100으로 옮긴다는 의미는 중심(평균)만 이동시키는 연산은 그냥 아래와 같습니다.
```
새 값 = 원래 값 + 이동량
```
여기서 이동량은 100 - 25 = +75

실제로 계산해 보면 원래 값들에 +75 적용
- 24 + 75 = 99
- 25 + 75 = 100
- 26 + 75 = 101
즉 결과는 99~101이 나옵니다.

그래서 결론은 25에서 ±1 만큼 흔들림이 있으나 평균 100에서 ±1 만큼 흔들림이 있으나 여전히 여전히 ±1 만큼 흔들립니다. 즉 위의 코드는 100근처의 정상 센서를 만들어내는 가짜 데이터 제조기라고 생각하면 됩니다.

이것은 이렇게 활용될 예정입니다 
정상처럼 보이는 값들을 먼저 만들고, 최근 정상 범위(평균 + 변동성)를 계산한 뒤,  
그 범위를 크게 벗어나는 값들을 이상으로 판단하여 알람을 줍니다.

---
위의 코드는 정상 코드를 만들어내는 생성기였다면 아래 코드는 일부 구간에 이상한 값(튀는 값)을 일부러 넣는 코드입니다.
```python
# 이상치(스파이크) 일부 삽입
spike_idx = [120, 121, 260, 400, 520]
values[spike_idx] += [12, -14, 18, -20, 15]
```

`spike_idx`의 리스트 데이터는 이상치를 넣을 위치 번호 지정
- 120번째 값
- 121번째 값
- 260번째 값
- ...
여기만 건드리겠다는 뜻입니다.

`values[spike_idx] +=`는 그 위치 값들을 갑자기 크게 변경하여 어떤 값에 +12 → 확 증가시키고 어떤 값에 -14 → 확 감소 시켜서 이상 수치를 만들어 냅니다.

---
```python
df = pd.DataFrame({"timestamp": timestamps, "value": values})
```
	지금 만든 데이터들을 표 형태로 정리하는 코드

결과
![[Pasted image 20260214201532.png]]

timestamps 시간 목록과 values 랜덤 숫자 목록으로 만든 데이터를 테이블에 저장합니다.

---
```python
# (실시간 데이터처럼 쓰려면, 항상 시간순 정렬이 기본)
df = df.sort_values("timestamp").reset_index(drop=True)
```
	테이블에 저장한 데이터를 시간순서대로 다시 정렬하는 코드입니다.

실시간 / 시계열 데이터의 가장 중요한 규칙:
- 시간 순서가 틀리면 분석이 망가짐
- 이상탐지, rolling 계산 전부 시간 순서에 의존
- timestamp 컬럼 기준으로 오름차순 정렬

`reset_index(drop=True)` 의미는 정렬하면 인덱스 번호가 꼬이므로 정렬한 기준으로 인덱스를 다시 0부터 매김

---
최근 몇 초를 기준으로 정상 패턴을 잡을지 정합니다.
- `WINDOW=30` → 최근 30개(1초 간격이면 최근 30초)
- `Z_THRESHOLD=3.0` → 보통 많이 쓰는 3시그마(3σ) 규칙
    
의미: 정상 기준선은 ‘최근 30초’로 잡고, 그 기준에서 3배 이상 튀면 경보.

(1) Rolling window 설정 (예: 최근 30개 = 최근 30초)
```python
WINDOW = 30          # 최근 30초(1초 간격이면 30개)
Z_THRESHOLD = 3.0    # 보통 3σ 규칙(상황에 따라 2.5~4 조절)
```

WINDOW = 기준을 계산할 데이터 개수  
Z_THRESHOLD = 이상이라고 판단하는 기준선

WINDOW = 30  
→ 최근 30개 값으로 평균 / 표준편차 계산

Z_THRESHOLD = 3.0
→ 얼마나 튀면 이상이라고 볼 것인가?
즉 Z-score는 현재값이 평균에서 얼마나 멀리 떨어졌는지 점수이며
- 값이 크면 → 많이 튐
- 값이 작으면 → 정상 범위
그럼 Z_THRESHOLD 역할은? 이상 판정 기준선입니다.

예를 들어 - Z_THRESHOLD = 3이면 평균에서 표준편차 3배 이상 벗어나면 이상수치로
센서 값이 평소 이런 범위라 가정하면
```
99 ~ 101
```
그런데 갑자기 115가 등장하면 

➡ Z-score 급증  
➡ 기준선 3 넘음  
➡ 이상치 판정

3이란 수치는 이상범위를 가상으로 만들어준 것이며 실제 센서데이터마다 이상수치의 범위가 지정되어 있어 그 수치를 시그마에 대입하면 됩니다.

---
최근 30초의 정상 기준선을 계산합니다.
- `roll_mean` = 최근 30초 평균 → 지금 정상이라면 이 정도
- `roll_std` = 최근 30초 표준편차 → 정상 범위에서 흔들리는 폭
    
그리고 `min_periods=WINDOW`를 둬서 초반 데이터(30개 전까지)는 계산하지 않게 함.

의미: 아직 데이터가 충분하지 않은 초반엔 기준이 불안정하니까 경보를 울리지 않게합니다

(2) Rolling 통계 계산 (rolling mean / rolling std)
```python
df["roll_mean"] = df["value"].rolling(window=WINDOW, min_periods=WINDOW).mean()
df["roll_std"]  = df["value"].rolling(window=WINDOW, min_periods=WINDOW).std(ddof=0)
```
	최근 WINDOW개 값으로 평균과 흔들림 크기를 계속 계산한다

- `window=WINDOW` : 최근 몇 개 데이터를 기준으로 계산할 것인가(계산범위설정)
- `min_periods=WINDOW` : 데이터가 30개 쌓이기 전까지는 계산하지 마라
- `.mean()` : 평균 계산 함수

---
지금 값이 “최근 기준”에서 얼마나 이상한지 점수로 만듦니다 (Z-score)
- `z_score = (현재값 - 최근평균) / 최근표준편차`
- 즉, 평균에서 표준편차 몇 배나 벗어났나?
    
EPS는 `roll_std`가 0에 가까울 때 분모가 0이 되는 걸 막는 안전장치.

의미: 그냥 튄 것 같다는 느낌이 아니라, ‘정량 점수’로 지금이 얼마나 튀었는지 계산합니다


(3) Z-score 계산
```python
EPS = 1e-9
df["z_score"] = (df["value"] - df["roll_mean"]) / (df["roll_std"] + EPS)
```
	표준편차(roll_std)가 0이 되는 순간, 나눗셈이 터지는 걸 막는 안전장치

뼈대 패턴에서 설명한 내용

---
규칙을 적용해서 이상이다/아니다를 결정합니다.
- `abs(z_score) >= 3` 이면 `is_anomaly=True`
    
의미: 점수가 기준선을 넘으면, 이 시점은 경보를 울릴 이벤트로 확정.


(4) 임계치(Alert) 규칙
```python
df["is_anomaly"] = df["z_score"].abs() >= Z_THRESHOLD
```
	Z-score가 기준값보다 크면 이상(True)으로 표시

`df["z_score"]` : 현재 값이 평균에서 얼마나 튀었는지의 점수
`.abs()` : 절대값 함수로 (+4)위로 튀어도 이상, (-4)아래로 튀어도 이상이므로 부호제거
`>= Z_THRESHOLD` : 이 정도 이상이면 경보 울려라 
- threshold = 3
- z_score = 2.5 → 정상
- z_score = 3.2 → 이상

![[Pasted image 20260214204010.png]]
해석
```
roll_mean → NaN  
roll_std → NaN  
z_score → NaN
```
	아직 30개(WINDOW) 데이터가 안 쌓였기 때문

아래쪽 숫자 부분 해석
```
value = 100.45  
roll_mean = 100.10  
roll_std = 1.07  
z_score = 0.32
```
의미:
✔ 현재 값이 평균 근처  
✔ 거의 안 튐  
✔ 정상 범위

Z-score는 3까지 정상 범위로 
- z_score = 0.3 → 정상
- z_score = 1.2 → 정상
- z_score = 2.8 → 정상
- z_score = 3.1 → 이상
이상 판정 조건:
```
abs(z_score) >= 3
```
절대값(abs)을 사용하므로 정상 범위 구간은
```
-3 < z_score < +3
```
이 구간은 정상입니다.

이 구간에서 이상수치 구간만 필터링 하려면
```python
anomalies = df[df["is_anomaly"]] 
# 또는 둘다 같은 코드입니다.
df[df["z_score"].abs() >= Z_THRESHOLD]
```
결과
![[Pasted image 20260214204738.png]]

---
결과를 출력하고, 그래프로 확인합니다.
- 알람이 총 몇 번 발생했는지
- 언제 발생했는지(timestamp)
- 그 순간 값이 얼마였고, 평균/표준편차/점수가 어떻게 나왔는지
    
그리고 그래프 3개로 눈으로 확인:
1. value vs roll_mean (튀는 순간 보임)
2. z_score (임계치 넘는 순간 보임)
3. anomaly point만 점으로 표시 (경보 시점만 콕)
    
의미: 알고리즘이 진짜로 스파이크를 잡았는지 사람이 한 번 검증할 수 있게 만듦니다.


(5) 결과 확인 (알람 요약 + 알람 발생 시점 출력)
```python
alarm_count = int(df["is_anomaly"].sum())
print(f"알람 개수: {alarm_count}개 (Z_THRESHOLD={Z_THRESHOLD}, WINDOW={WINDOW})")
```
결과
```
알람 개수: 6개 (Z_THRESHOLD=3.0, WINDOW=30)
```

알람이 발생한 행만 따로 확인 (언제/얼마나 튀었는지)
```python
alarm_rows = df[df["is_anomaly"]].copy()
print(alarm_rows[["timestamp", "value", "roll_mean", "roll_std", "z_score"]].head(10))
```
결과
```
              timestamp       value   roll_mean  roll_std   z_score
120 2026-02-11 10:02:00  112.949238  100.403456  2.564473  4.892149
121 2026-02-11 10:02:01   84.908735   99.861668  3.777124 -3.958814
209 2026-02-11 10:03:29  104.623278  100.078711  1.242113  3.658739
260 2026-02-11 10:04:20  116.887683  100.694794  3.244782  4.990440
400 2026-02-11 10:06:40   78.086687   99.517731  4.207186 -5.093915
520 2026-02-11 10:08:40  112.996714  100.334099  2.566091  4.934593
```


---
(선택) 시각화 1) 값(value) + 롤링 평균(roll_mean)
```python
fig1 = px.line(df, x="timestamp", y=["value", "roll_mean"], title="Sensor value vs Rolling Mean")
fig1.show()
```
결과
![[Pasted image 20260214203608.png]]
- 파란선 (value) → 실제 센서 값
- 주황선 (roll_mean) → 최근 구간 평균(정상 기준선)
즉 현재 값 vs 정상 기준선 비교 그래프입니다.

파란선이 주황선 주변에서 움직임:
✔ 정상 상태  
✔ 자연스러운 노이즈  
✔ 문제 없음

삐죽 튀어나온 구간 의미
보이는 급격한 위/아래 스파이크:
✔ 평소 패턴과 다름  
✔ 순간적으로 크게 변함  
✔ 이상 후보 구간

하지만 눈에 튄다고 무조건 이상은 아니며 
✔ Z-score 계산  
✔ 표준편차 대비 거리  
✔ Threshold 비교로 결정됩니다.

---
(선택) 시각화 2) Z-score 그래프
```python
fig2 = px.line(df, x="timestamp", y="z_score", title="Z-score over time")
fig2.show()
```
결과
![[Pasted image 20260214203627.png]]
이 그래프가 보여주는 것은 시간 흐름에 따라 얼마나 튀었는지 점수 변화입니다.

세로축 (z_score)의 의미
✔ 0 근처 → 정상  
✔ 값 커짐 → 평균에서 멀어짐  
✔ ± 방향 모두 중요

그래프 대부분 구간은
✔ 0 근처에서 흔들림  
✔ 작은 진동  
✔ 정상 상태

튀는 구간은 보이는 급격한 피크들입니다
✔ 위로 튀는 값 → 큰 양수  
✔ 아래로 튀는 값 → 큰 음수
이 부분들이 평균 대비 갑자기 크게 벗어난 순간입니다. 즉 이상 후보 지점입니다.

---
(선택) 시각화 3) 알람 포인트(이상치)만 점으로 표시
```python
alarm_plot = df[df["is_anomaly"] & df["roll_mean"].notna()].copy()
fig3 = px.scatter(alarm_plot, x="timestamp", y="value", title="Anomaly points (Threshold Alert)")
fig3.show()
```
결과
![[Pasted image 20260214203644.png]]
이상으로 판정된 값만 찍은 그래프

여기서는 더 이상 전체 흐름이 목적이 아니고 이상한 지점만 확인하기 위함입니다.
즉 실제고 경보를 울려야 하는 순간들일수 있습니다.